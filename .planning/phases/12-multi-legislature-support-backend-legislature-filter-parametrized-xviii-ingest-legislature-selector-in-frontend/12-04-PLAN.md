---
phase: 12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified: []
autonomous: false
requirements: [LEG-04]
must_haves:
  truths:
    - "The human explicitly approves launching the long ingest before it starts"
    - "XVIII Camera sessions (legislature=18) exist in Neo4j after the run"
    - "XVIII Senate sessions (legislature=18) exist in Neo4j after the run"
    - "XIX query results with default legislature=19 are unchanged after the ingest (no contamination)"
  artifacts:
    - path: ".planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-04-SUMMARY.md"
      provides: "Ingest run record with final Cypher counts"
      contains: "legislature = 18"
  key_links:
    - from: "make db-ingest-leg18"
      to: "Neo4j Session {legislature: 18}"
      via: "update --legislature 18 + update-senate --legislature 18"
      pattern: "legislature 18"
---

<objective>
Execute the additive XVIII (leg18) ingest into the live Neo4j database and verify the counts, AFTER the backend legislature filter (Plans 01-02) is live and the build pipeline is parametrized (Plan 03). This is the long-running, money-spending step (~2-4h machine time, ~$2-3 embeddings, shielded by the embedding cache at build/embeddings_cache.db) — so it is gated behind an explicit human decision.

Purpose: Populate the graph with 741 Camera + 459 Senate XVIII sittings so the frontend selector (Plan 05) has data to switch to, without contaminating XIX answers.
Output: XVIII Session nodes (legislature=18) in Neo4j; verified counts; a run record.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-CONTEXT.md
@.planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-RESEARCH.md

<preconditions>
Before this plan runs, the following MUST be true (verify, do not assume):
- Plans 01 and 02 merged: backend retrieval + timeline filter by s.legislature (default 19) — so ingesting XVIII cannot contaminate default-19 queries.
- Plan 03 merged: `make db-ingest-leg18` target exists and `build_and_update.py update --legislature 18` works.
- Raw XVIII data on disk (already downloaded per CONTEXT): `data/xml/stenografico_leg18_*.xml` (741 files), `data/senate_xml/resaula_leg18_*.akn` (459 files), `data/deputati_xviii*.csv`, `data/senatori_xviii*.csv`.
- Neo4j running at bolt://localhost:7689 with the current XIX database (1,111 Session nodes, all legislature=19).

Expected final counts (RESEARCH-verified targets): Camera leg18 = 741 sessions, Senate leg18 = 459 sessions.
</preconditions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve launch of the XVIII ingest</name>
  <action>Gather the precondition numbers below (make -n dry-run output, on-disk leg18 file counts, baseline Cypher count of legislature=18 sessions) and present them to the human alongside the launch decision. Do NOT run Task 2 until the human types "launch-now".</action>
  <decision>Launch the XVIII (leg18) ingest now?</decision>
  <context>
This runs `make db-ingest-leg18`, an additive (no-nuke) ingest of 741 Camera XMLs + 459 Senate AKNs. It costs approximately 2-4 hours of machine time and ~$2-3 in OpenAI embeddings (the embedding cache at build/embeddings_cache.db shields any repeated chunks). It writes ~1,200 new Session nodes with legislature=18 alongside the existing XIX data. XIX answers stay unchanged because the backend now filters by legislature=19 by default (Plans 01-02).

Before approving, the executor MUST confirm preconditions with a dry run and count checks:
- `make -n db-ingest-leg18` prints the two `update --legislature 18` / `update-senate --legislature 18` commands.
- `ls data/xml/stenografico_leg18_*.xml | wc -l` ~ 741 and `ls data/senate_xml/resaula_leg18_*.akn | wc -l` ~ 459.
- Cypher baseline: `MATCH (s:Session {legislature: 18}) RETURN count(s)` returns 0 (nothing ingested yet).
Report these numbers to the human as part of the decision prompt.
  </context>
  <options>
    <option id="launch-now">
      <name>Launch now</name>
      <pros>Data ready for frontend selector + E2E verification; embeddings cache warm.</pros>
      <cons>2-4h run; ~$2-3 cost; machine busy during ingest.</cons>
    </option>
    <option id="defer">
      <name>Defer</name>
      <pros>Run later at a convenient time (e.g. overnight).</pros>
      <cons>Frontend selector (Plan 05) can ship but has no XVIII data to switch to; E2E (Plan 06) blocked.</cons>
    </option>
  </options>
  <resume-signal>Type "launch-now" to run the ingest, or "defer" to stop here (Plan 05 can still proceed in parallel).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the additive XVIII ingest, relink, and verify counts</name>
  <files>(no source files — this task runs the ingest and verifies the DB)</files>
  <read_first>
    - Makefile (db-ingest-leg18 target created in Plan 03)
    - build/build_and_update.py (do_update / do_update_senate — confirm --legislature 18 path)
  </read_first>
  <action>
Only run this after the human approved "launch-now" in Task 1.

1. Baseline count (before): run Cypher `MATCH (s:Session {legislature: 18}) RETURN count(s) AS n` — record n (expected 0).
2. Launch the ingest: `make db-ingest-leg18`. This runs `update --legislature 18 --skip-download` (Camera) then `update-senate --legislature 18 --skip-download` (Senate). Run it as a long-running/background command since it takes 2-4h; stream progress logs. The update modes use the legislature-filtered `get_existing_session_numbers` so all 741 leg18 Camera + 459 leg18 Senate sessions are treated as new.
3. The senate update mode already calls `relink_senate_speeches()` at the end; also confirm Camera speeches linked. If any orphan-speech relink is needed for Camera leg18, run the appropriate relink step (the Camera SPOKEN_BY linker fix from CONTEXT: `db_builder._link_speeches_to_speakers`).
4. Verify final counts with Cypher:
   - `MATCH (s:Session) WHERE s.legislature = 18 RETURN s.chamber AS ch, count(*) AS n` → expect camera=741, senato=459 (senate may be 459; a few AKNs were never published — CONTEXT notes 6 missing, so 459 is the on-disk count).
   - `MATCH (s:Session) WHERE s.legislature = 19 RETURN count(s)` → expect unchanged (1,111 total across chambers as before ingest — confirm XIX untouched).
5. Contamination regression check: with the backend running (or via direct Cypher matching the dense-channel path), confirm a default legislature=19 retrieval returns NO leg18 chunks. Quick Cypher proxy: `MATCH (s:Session {legislature: 19})-[:HAS_DEBATE]->()-[:HAS_PHASE]->()-[:CONTAINS_SPEECH]->()-[:HAS_CHUNK]->(c) WITH count(c) AS xixChunks RETURN xixChunks` before and after — should be identical.
6. Record all counts in the SUMMARY.

If the ingest fails partway, the update mode is resumable (dedup by legislature-filtered session numbers) — re-run `make db-ingest-leg18` to continue from where it stopped.
  </action>
  <verify>
    <automated>echo "Manual Cypher verification — see acceptance criteria. Automated gate: the two update commands exit 0 and MATCH (s:Session {legislature:18}) RETURN count(s) > 1000."</automated>
  </verify>
  <done>XVIII Camera (~741) and Senate (~459) sessions exist with legislature=18; XIX counts unchanged; no leg18 chunks reachable from a legislature=19 session.</done>
  <acceptance_criteria>
    - Cypher `MATCH (s:Session {chamber:'camera', legislature:18}) RETURN count(s)` returns 741 (± the exact on-disk XML count)
    - Cypher `MATCH (s:Session {chamber:'senato', legislature:18}) RETURN count(s)` returns 459 (± the exact on-disk AKN count)
    - Cypher `MATCH (s:Session {legislature:19}) RETURN count(s)` returns the same value as before the ingest (XIX untouched)
    - The `update --legislature 18` and `update-senate --legislature 18` commands both exited 0
    - SUMMARY records baseline (0) and final leg18 counts
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `MATCH (s:Session) WHERE s.legislature = 18 RETURN s.chamber, count(*)` → camera 741, senato 459.
- `MATCH (s:Session) WHERE s.legislature = 19 RETURN count(s)` → unchanged vs pre-ingest.
- Backend query with default (legislature=19) returns only 2022-2027 XIX citations (no 2018-2022 dates).
</verification>

<success_criteria>
- Human explicitly approved the launch.
- XVIII Camera + Senate sessions ingested with legislature=18.
- XIX data and default-19 query results verified unchanged (no contamination).
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-04-SUMMARY.md` with the baseline and final Cypher counts.
</output>
