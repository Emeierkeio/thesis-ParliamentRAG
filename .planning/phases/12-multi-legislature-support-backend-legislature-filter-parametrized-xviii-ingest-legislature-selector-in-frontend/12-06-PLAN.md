---
phase: 12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend
plan: 06
type: execute
wave: 3
depends_on: ["04", "05"]
files_modified: []
autonomous: false
requirements: [LEG-06]
must_haves:
  truths:
    - "Selecting XVIII and asking an XVIII-era question returns citations dated 2018-2022"
    - "Selecting XIX returns the same answers as before XVIII was ingested (no contamination)"
    - "The badge and selector reflect the active legislature"
  artifacts:
    - path: ".planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-06-SUMMARY.md"
      provides: "E2E verification record"
      contains: "XVIII"
  key_links:
    - from: "LegislatureSelector (frontend)"
      to: "backend retrieval legislature filter"
      via: "fetch payload legislature → s.legislature = $legislature"
      pattern: "legislature"
---

<objective>
End-to-end verification that the full legislature dimension works: frontend selector → fetch payload → backend filter → legislature-scoped citations. Confirms XVIII queries return 2018-2022 content and XIX answers are unchanged after ingest. This is the phase gate.

Purpose: Prove no cross-legislature contamination and that the selector actually switches the corpus.
Output: A human-verified E2E record.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-CONTEXT.md

<preconditions>
- Plan 04 complete: XVIII Session nodes (legislature=18) in Neo4j (camera 741, senato 459).
- Plan 05 complete: LegislatureSelector wired, fetch payload carries legislature.
- Backend + frontend running (`make dev` or equivalent).
</preconditions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checks before human verification</name>
  <files>(no source changes — runs the test suite and Cypher sanity checks)</files>
  <read_first>
    - .planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-04-SUMMARY.md (ingest counts)
  </read_first>
  <action>
Run the automated gates and capture output for the checkpoint:
1. `cd backend && python -m pytest tests/ -q` — full backend suite green.
2. `cd frontend && npx tsc --noEmit` — zero type errors.
3. Cypher counts: `MATCH (s:Session) WHERE s.legislature = 18 RETURN s.chamber, count(*)` (expect camera 741 / senato 459) and `MATCH (s:Session) WHERE s.legislature = 19 RETURN count(s)` (expect unchanged vs pre-ingest baseline in 12-04-SUMMARY).
4. Confirm backend + frontend are up (curl the query endpoint health / load the chat page).
Record all outputs; if any gate fails, STOP and report before the human checkpoint.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Full backend suite green; frontend type-clean; Cypher counts match Plan 04 targets.</done>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/ -q` exits 0
    - `cd frontend && npx tsc --noEmit` exits 0
    - Cypher leg18 counts match 12-04-SUMMARY (camera 741, senato 459)
    - Cypher leg19 count equals the pre-ingest baseline recorded in 12-04-SUMMARY
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human E2E verification of the legislature switch</name>
  <action>Present the what-built summary and the numbered how-to-verify steps to the human, with backend and frontend running. Wait for the human's approval (or issue report) before writing the SUMMARY.</action>
  <what-built>
A legislature selector (XVIII | XIX) next to the chamber selector. It sends the chosen legislature in the query payload; the backend filters every retrieval channel by `s.legislature = $legislature`. XVIII data (741 Camera + 459 Senate sittings, 2018-2022) is ingested alongside XIX. Default is XIX so existing behavior is unchanged.
  </what-built>
  <how-to-verify>
1. Open the chat page. Confirm the legislature selector shows XVIII and XIX, defaulting to XIX. The welcome badge reads "... · XIX Legislatura/Legislature".
2. With XIX selected, ask an XIX-era question (e.g. "decreto PNRR 2023"). Confirm citations are dated 2022-10-13 or later (XIX). Note the answer.
3. Switch the selector to XVIII. Confirm the badge updates to "... · XVIII Legislatura/Legislature" and the URL gains `?legislature=18`.
4. Ask an XVIII-era question (e.g. "decreti COVID" or "decreto rilancio"). Confirm citations are dated 2018-03-23 → 2022-10-12 (XVIII). No 2023+ dates should appear.
5. Reload the page — confirm XVIII persists (localStorage + URL).
6. Switch back to XIX and re-ask the step-2 question — confirm the answer/citations match step 2 (no contamination from XVIII).
7. Optional: toggle chamber (Camera/Senato/Both) together with legislature to confirm both filters compose.
  </how-to-verify>
  <resume-signal>Type "approved" if XVIII returns 2018-2022 citations and XIX is unchanged, or describe any contamination / UI issues observed.</resume-signal>
</task>

</tasks>

<verification>
- XVIII query returns only 2018-2022 citations; XIX query unchanged vs pre-ingest.
- Selector persists across reload; badge reflects selection.
- Chamber + legislature filters compose.
</verification>

<success_criteria>
- Human confirms the legislature switch works end-to-end with no cross-legislature contamination.
</success_criteria>

<output>
After completion, create `.planning/phases/12-multi-legislature-support-backend-legislature-filter-parametrized-xviii-ingest-legislature-selector-in-frontend/12-06-SUMMARY.md` with the E2E results.
</output>
