---
phase: 03-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/sse.ts
  - frontend/src/types/chat.ts
  - frontend/src/types/survey.ts
  - frontend/src/types/api.ts
  - frontend/src/hooks/use-chat.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/chat/CompassCard.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/graph/GraphVisualizer.tsx
  - frontend/src/components/shared/HistoryModal.tsx
  - frontend/src/components/shared/ProgressIndicator.tsx
  - frontend/src/components/settings/SettingsModal.tsx
  - frontend/src/components/survey/SurveyModal.tsx
  - frontend/src/app/ranking/page.tsx
  - frontend/src/app/explorer/page.tsx
  - frontend/src/app/compass/page.tsx
  - frontend/src/app/valutazione/page.tsx
autonomous: true
requirements:
  - FE-01
must_haves:
  truths:
    - "tsc --noEmit compiles with zero errors"
    - "grep for ': any' in src/ returns zero matches"
    - "Every SSE event payload has a typed interface derived from SSE_CONTRACT.md"
  artifacts:
    - path: "frontend/src/types/sse.ts"
      provides: "SSE event discriminated union types"
      exports: ["SSEEvent", "SSEWaitingEvent", "SSEProgressEvent", "SSEExpertsChatEvent", "SSEChunkChatEvent", "SSECitationsChatEvent", "SSECommissioniEvent", "SSEBalanceEvent", "SSECompassChatEvent", "SSETopicStatsEvent", "SSECompleteEvent", "SSEErrorEvent", "CommissionItem"]
    - path: "frontend/src/types/chat.ts"
      provides: "Updated chat types — no any, StepResultDetails union, ChatHistoryItem interface"
      contains: "StepResultDetails"
  key_links:
    - from: "frontend/src/hooks/use-chat.ts"
      to: "frontend/src/types/sse.ts"
      via: "import SSE types for event parsing"
      pattern: "import.*from.*types/sse"
    - from: "frontend/src/components/shared/HistoryModal.tsx"
      to: "frontend/src/types/chat.ts"
      via: "import ChatHistoryItem"
      pattern: "ChatHistoryItem"
---

<objective>
Eliminate all `any` types from the frontend codebase and create proper TypeScript interfaces for all SSE event payloads.

Purpose: FE-01 requires strict TypeScript with zero `any`. The 50 `any` occurrences across 15 files must be replaced with proper types. The SSE event interfaces (derived from SSE_CONTRACT.md) are the single largest source of `any` in use-chat.ts.

Output: `frontend/src/types/sse.ts` with full SSE discriminated union, updated type files, and zero `any` across all 15 affected files.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend/03-CONTEXT.md
@.planning/phases/03-frontend/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SSE type interfaces and fix type-level any in type files</name>
  <files>
    frontend/src/types/sse.ts,
    frontend/src/types/chat.ts,
    frontend/src/types/survey.ts,
    frontend/src/types/api.ts
  </files>
  <read_first>
    backend/docs/SSE_CONTRACT.md,
    frontend/src/types/chat.ts,
    frontend/src/types/survey.ts,
    frontend/src/types/api.ts,
    frontend/src/components/chat/CompassCard.tsx,
    frontend/src/hooks/use-chat.ts
  </read_first>
  <action>
**1. Create `frontend/src/types/sse.ts`** with the full SSE discriminated union as specified in 03-RESEARCH.md "Pattern 2: SSE Event Discriminated Union" section. The exact interfaces are listed in the research at lines 162-298. Key types:
- SSEWaitingEvent, SSEProgressEvent
- SSEExpertsChatEvent (experts field), SSEExpertsQueryEvent (data field)
- SSEChunkChatEvent (content field), SSEChunkQueryEvent (data field)
- SSECitationsChatEvent (citations field), SSECitationsQueryEvent (data field)
- SSECommissioniEvent with CommissionItem interface
- SSEBalanceEvent with maggioranza_percentage, opposizione_percentage, bias_score (these are frozen wire names from backend)
- SSECompassChatEvent, SSECompassQueryEvent
- SSETopicStatsEvent, SSECitationDetailsEvent, SSEHQVariantsEvent
- SSECompleteEvent, SSEErrorEvent
- Export the union type `SSEEvent`

Import types from `./chat` where needed (Expert, Citation, SpeakerDetail, InterventionDetail, SessionDetail, HQVariant). Import CompassData from `../components/chat/CompassCard`.

**2. Fix `frontend/src/types/chat.ts`:**
- Line 26: Remove `[key: string]: any` index signature on Citation. Add any missing optional fields explicitly (check what callers actually access).
- Line 152: Replace `details?: any` on StepResult with `details?: Record<string, unknown>` (step details are polymorphic).
- Add `ChatHistoryItem` interface for the history API response shape — inspect `use-chat.ts` loadChat function (line 669+) to see what fields historyData has: `answer`, `experts`, `citations`, `steps`, `balance`, `compass`, `commissioni`, `hq_variants`, `topic_stats`.

**3. Fix `frontend/src/types/survey.ts`:**
- Line 215: Replace `baseline_experts?: any[]` with `baseline_experts?: Expert[]` — import Expert from `./chat`.

**4. Fix `frontend/src/types/api.ts`:**
- Line 2: Translate Italian docstring `Tipi per le API` to `API types` (this is code-level Italian, not UI text).
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/sse.ts` exists and exports SSEEvent union type
    - `grep -c ": any" frontend/src/types/chat.ts` returns 0
    - `grep -c ": any" frontend/src/types/survey.ts` returns 0
    - `grep -c "any\b" frontend/src/types/api.ts` returns 0
    - ChatHistoryItem interface exists in chat.ts
    - StepResult.details is not typed as `any`
  </acceptance_criteria>
  <done>All 4 type files have zero `any`. SSE discriminated union exists in sse.ts with all event types from SSE_CONTRACT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all any from hooks, components, and page files</name>
  <files>
    frontend/src/hooks/use-chat.ts,
    frontend/src/lib/api.ts,
    frontend/src/components/chat/CompassCard.tsx,
    frontend/src/components/chat/MessageBubble.tsx,
    frontend/src/components/graph/GraphVisualizer.tsx,
    frontend/src/components/shared/HistoryModal.tsx,
    frontend/src/components/shared/ProgressIndicator.tsx,
    frontend/src/components/settings/SettingsModal.tsx,
    frontend/src/components/survey/SurveyModal.tsx,
    frontend/src/app/ranking/page.tsx,
    frontend/src/app/explorer/page.tsx,
    frontend/src/app/compass/page.tsx,
    frontend/src/app/valutazione/page.tsx
  </files>
  <read_first>
    frontend/src/hooks/use-chat.ts,
    frontend/src/lib/api.ts,
    frontend/src/components/chat/CompassCard.tsx,
    frontend/src/components/chat/MessageBubble.tsx,
    frontend/src/components/graph/GraphVisualizer.tsx,
    frontend/src/components/shared/HistoryModal.tsx,
    frontend/src/components/shared/ProgressIndicator.tsx,
    frontend/src/components/settings/SettingsModal.tsx,
    frontend/src/components/survey/SurveyModal.tsx,
    frontend/src/app/ranking/page.tsx,
    frontend/src/app/explorer/page.tsx,
    frontend/src/app/compass/page.tsx,
    frontend/src/app/valutazione/page.tsx,
    frontend/src/types/sse.ts,
    frontend/src/types/chat.ts
  </read_first>
  <action>
Replace every `any` in these 13 files using the exact replacement map from 03-RESEARCH.md "Complete any Map" section. File-by-file:

**use-chat.ts (11 any):**
- Line 178: `let commissioni: any[]` -> `let commissioni: CommissionItem[]` (import from types/sse)
- Lines 180, 261: `Map<number, { step...; details?: any }>` -> `Map<number, StepResult>` (import from types/chat)
- Line 297: `commList.map((c: any) => ...)` -> `(c: CommissionItem)`
- Line 484: `Map<number, any>` -> `Map<number, StepResult>`
- Line 669: `loadChat(historyData: any)` -> `loadChat(historyData: ChatHistoryItem)`
- Line 704: `details?: any` -> use StepResult[]
- Lines 708, 717-719: type annotations on map/filter callbacks -> use ChatHistoryItem typed fields (citations: Citation[], experts: Expert[])

**lib/api.ts (1 any):**
- Line 45: `function mapRawToConfig(raw: any)` -> `raw: unknown` with runtime narrowing, or type-assert to the raw config shape

**CompassCard.tsx (2 any):**
- Line 33: `stats: any` -> `stats: Record<string, number>`
- Line 166: `style?: any` -> `style?: React.CSSProperties`

**MessageBubble.tsx (1 any):**
- Line 841: `{ variant: any }` -> `{ variant: HQVariant }` (import from types/chat)

**GraphVisualizer.tsx (8 any):**
- Line 16: `data: any[]` -> define `GraphRecord` interface (object with string keys and primitive/object values — inspect the component to see what fields are accessed)
- Line 20: `{ nodes: any[]; links: any[] }` -> `{ nodes: GraphNode[]; links: GraphLink[] }` (define interfaces with id, label, color, val, x, y fields for nodes; source, target for links)
- Line 24: `useRef<any>(null)` -> `useRef<ForceGraphMethods | null>(null)` — import ForceGraphMethods from `react-force-graph-2d` (check if the package exports it; if not, use the return type of the ForceGraph2D component ref)
- Line 27: `useState<any>(null)` -> `useState<GraphNode | null>(null)`
- Line 45: `processItem = (item: any)` -> `processItem = (item: GraphRecord)`
- Line 205: `handleNodeClick = (node: any)` -> `handleNodeClick = (node: NodeObject)` from react-force-graph-2d
- Line 248: `(node: any, ctx, globalScale)` -> `(node: NodeObject, ctx: CanvasRenderingContext2D, globalScale: number)`
- Line 273: `(node: any, color, ctx)` -> `(node: NodeObject, color: string, ctx: CanvasRenderingContext2D)`

**HistoryModal.tsx (2 any):**
- Line 35: `onLoadChat?: (chat: any) => void` -> `onLoadChat?: (chat: ChatHistoryItem) => void`
- Line 51: `useState<any[]>([])` -> `useState<ChatHistoryItem[]>([])`

**ProgressIndicator.tsx (1 any):**
- Line 57: `details?: any` -> `details?: Record<string, unknown>`

**SettingsModal.tsx (1 any):**
- Line 121: `let parsed: any` -> `let parsed: SystemConfig` (import or define the config type matching what mapRawToConfig returns in lib/api.ts)

**SurveyModal.tsx (9 any):**
- Line 72: `citations: any[]` -> `citations: Citation[]`
- Line 74: `balance: any` -> `balance: BalanceMetrics` (from types/chat)
- Line 75: `compass: any` -> `compass: CompassData | null`
- Line 833: `citations?: any[]` -> `citations?: Citation[]`
- Line 857: `as any[]` cast -> remove cast, use proper typing
- Lines 875, 894: `.map((c: any) => ...)` -> `.map((c: Citation) => ...)`
- Lines 1079, 1109: `err: any` -> `err: unknown` + `if (err instanceof Error) { err.message } else { String(err) }`

**ranking/page.tsx (2 any):**
- Line 126: `data.deputies.map((d: any) => ...)` -> define local `RawDeputyApiItem` interface by inspecting what fields are accessed
- Line 134: `catch (e: any)` -> `catch (e: unknown)` + instanceof Error guard

**explorer/page.tsx (4 any):**
- Line 33: `useState<any>(null)` for schema -> define `GraphSchema` interface
- Line 34: `useState<any>(null)` for stats -> define `GraphStats` interface
- Line 36: `useState<any[]>([])` for results -> define `GraphQueryResult` interface
- Line 277: `(val: any, j)` -> `(val: unknown, j)`

**compass/page.tsx (4 any):**
- Line 62: `groups: any[]` -> `groups: CompassData["groups"]`
- Line 63: `scatter_sample: any[]` -> `scatter_sample: CompassData["scatter_sample"]`
- Line 99: `catch (e: any)` -> `catch (e: unknown)` + instanceof Error guard
- Line 402: `as any` cast -> fix the state type so it matches CompassData directly

**valutazione/page.tsx (1 any):**
- Line 1236: `(item.human as any)[dim]` -> use keyof-based type-safe lookup: define dimension keys type and cast appropriately

After all replacements, run `cd frontend && npx tsc --noEmit` to verify compilation. Fix any cascading type errors that arise from the `any` removals (e.g., missing optional chaining, null checks needed by strictNullChecks).
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -3 && echo "---" && grep -rn ": any\b" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "// @ts-expect-error" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (zero type errors)
    - `grep -rn ": any\b" src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
    - No `as any` casts remain in source files
    - GraphVisualizer.tsx uses typed GraphNode/GraphLink interfaces
    - All catch blocks use `unknown` not `any`
  </acceptance_criteria>
  <done>Zero `any` occurrences in the entire frontend src/ directory. tsc --noEmit compiles cleanly with strict: true.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `grep -rn ": any\b" frontend/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
3. `grep -rn "as any" frontend/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
4. `frontend/src/types/sse.ts` exists with SSEEvent union type
</verification>

<success_criteria>
- Strict TypeScript compilation passes with zero errors
- Zero `any` types remain in the codebase (checked via grep)
- All SSE event payloads have typed interfaces matching SSE_CONTRACT.md
- No behavioral changes to the application
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md`
</output>
