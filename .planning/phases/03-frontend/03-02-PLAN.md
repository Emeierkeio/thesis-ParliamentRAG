---
phase: 03-frontend
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/types/chat.ts
  - frontend/src/hooks/use-chat.ts
  - frontend/src/app/api/chat/route.ts
  - frontend/src/app/valutazione/page.tsx
  - frontend/src/app/evaluation/page.tsx
  - frontend/src/app/ranking/page.tsx
  - frontend/src/app/rankings/page.tsx
  - frontend/src/app/explorer/page.tsx
  - frontend/src/app/explore/page.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/config/index.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/graph-api.ts
  - frontend/src/components/evaluation/index.ts
  - frontend/src/components/graph/index.ts
  - frontend/src/components/search/index.ts
  - frontend/src/components/settings/index.ts
  - frontend/src/components/ui/index.ts
autonomous: true
requirements:
  - FE-02
  - FE-03
must_haves:
  truths:
    - "Navigating to /valutazione redirects to /evaluation via client-side router.replace"
    - "Navigating to /ranking redirects to /rankings"
    - "Navigating to /explorer redirects to /explore"
    - "All code-level Italian variable names and comments are in English"
    - "User-facing UI text remains in Italian"
    - "No dead code remains (formatEventForFrontend, addUserMessage exported but unused)"
    - "Every component folder has a barrel index.ts"
  artifacts:
    - path: "frontend/src/app/evaluation/page.tsx"
      provides: "Evaluation page (moved from valutazione)"
      min_lines: 100
    - path: "frontend/src/app/rankings/page.tsx"
      provides: "Rankings page (moved from ranking)"
      min_lines: 50
    - path: "frontend/src/app/explore/page.tsx"
      provides: "Explore page (moved from explorer)"
      min_lines: 50
    - path: "frontend/src/app/valutazione/page.tsx"
      provides: "Client-side redirect to /evaluation"
      contains: "router.replace"
    - path: "frontend/src/components/evaluation/index.ts"
      provides: "Barrel export for evaluation components"
      contains: "export"
    - path: "frontend/src/components/graph/index.ts"
      provides: "Barrel export for graph components"
      contains: "export"
    - path: "frontend/src/components/search/index.ts"
      provides: "Barrel export for search components"
      contains: "export"
    - path: "frontend/src/components/settings/index.ts"
      provides: "Barrel export for settings components"
      contains: "export"
  key_links:
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/src/app/rankings/page.tsx"
      via: "href links updated from /ranking to /rankings"
      pattern: "/rankings"
    - from: "frontend/src/app/valutazione/page.tsx"
      to: "frontend/src/app/evaluation/page.tsx"
      via: "router.replace redirect"
      pattern: "router.replace.*evaluation"
---

<objective>
Rename Italian routes to English with client-side redirects, translate all code-level Italian to English, remove dead code, and add barrel exports to all component folders.

Purpose: FE-02 (clean component structure, dead code removal, barrel exports) and FE-03 (English routes and variable names). This plan depends on Plan 01 because some renamed Italian variables (like `commissioni` in types, `maggioranzaPercentage`/`opposizionePercentage` in BalanceMetrics) touch the same type files that Plan 01 modifies.

Output: English route structure with redirects, clean English-only code identifiers, no dead code, barrel exports for all 5 missing folders.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend/03-CONTEXT.md
@.planning/phases/03-frontend/03-RESEARCH.md
@.planning/phases/03-frontend/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Italian code identifiers, translate comments, remove dead code</name>
  <files>
    frontend/src/types/chat.ts,
    frontend/src/hooks/use-chat.ts,
    frontend/src/app/api/chat/route.ts,
    frontend/src/config/index.ts,
    frontend/src/lib/api.ts,
    frontend/src/lib/graph-api.ts,
    frontend/src/app/ranking/page.tsx,
    frontend/src/types/survey.ts
  </files>
  <read_first>
    frontend/src/types/chat.ts,
    frontend/src/hooks/use-chat.ts,
    frontend/src/app/api/chat/route.ts,
    frontend/src/config/index.ts,
    frontend/src/lib/api.ts,
    frontend/src/lib/graph-api.ts,
    frontend/src/app/ranking/page.tsx,
    frontend/src/types/survey.ts,
    .planning/phases/03-frontend/03-RESEARCH.md
  </read_first>
  <action>
**CRITICAL DISTINCTION:** Code identifiers (variable names, type property names, comments, docstrings) become English. Wire-format values from the backend SSE contract (like `maggioranza_percentage`, `opposizione_percentage`, `commissioni` as SSE event field names, and `coalition: "maggioranza"` comparison strings) MUST stay as-is because they are frozen backend contract values.

**1. types/chat.ts — Type-level Italian renames (cascading):**
- Rename `maggioranzaPercentage` property on `BalanceMetrics` to `majorityPercentage`
- Rename `opposizionePercentage` property on `BalanceMetrics` to `oppositionPercentage`
- Rename `commissioni` property on `Message` interface to `committeeMatches` and type it as `CommissionItem[]` (import from types/sse)
- Translate comment line 121: `// Metadati opzionali per le risposte dell'assistente` -> `// Optional metadata for assistant responses`
- Translate comment line 136: `// -1 = tutto opposizione...` -> `// -1 = all opposition, 0 = balanced, 1 = all majority`
- Translate docstring line 2: `Tipi per il sistema di chat` -> `Types for the chat system`

**2. hooks/use-chat.ts — Variable renames and comment translations:**
- `let commissioni` -> `let committeeMatches` (line 178, already retyped in Plan 01)
- `const commList` -> `const committeeList` (line 294)
- `commissioni = commList` -> `committeeMatches = committeeList` (line 295)
- `updateLastAssistantMessage({ commissioni: ... })` -> `updateLastAssistantMessage({ committeeMatches: ... })`
- `const commNames` -> `const committeeNames` (line 297)
- `const topComm` -> `const topCommittee` (line 299)
- `const commResult` -> `const committeeResult` (line 300)
- `const magg` -> `const majorityCount` (line 324)
- `const opp` -> `const oppositionCount` (line 325)
- `maggioranzaPercentage: data.maggioranza_percentage` -> `majorityPercentage: data.maggioranza_percentage` (line 374 — note: RHS is the wire name, stays Italian)
- `opposizionePercentage: data.opposizione_percentage` -> `oppositionPercentage: data.opposizione_percentage` (line 375)
- Translate all Italian comments (lines 214, 217, and any others)
- **Remove dead code:**
  - Remove `addUserMessage` function definition and remove from hook return value (confirmed unused by any component via grep)
  - Remove empty `else {}` blocks at lines 343, 368
  - Remove empty `if (data.metadata?.timing)` block at lines 512-513
  - Remove empty `unmatchedLinks` if/else branches at lines 475-479

**3. app/api/chat/route.ts — Dead code removal and comment translation:**
- **DELETE** `formatEventForFrontend`, `formatExpert`, and `formatCitation` functions (lines 117-226 approximately) — confirmed dead code, never called
- Translate all Italian comments to English (lines 4, 6-8, 24, 38, 48, 55-78, 109, 231, 241, 244, 259, 283, 287) — use the exact translations from 03-RESEARCH.md "Variable-Level Italian in app/api/chat/route.ts" table
- Replace Italian error string `"Errore durante l'elaborazione"` with `"Error during processing"` (this is in mock fallback, not user-facing UI)

**4. config/index.ts:**
- Translate Italian docstring (lines 2-3): `Configurazione centralizzata dell'applicazione / Modifica questi valori...` -> English

**5. lib/api.ts:**
- Line 78: Replace `Errore nel caricamento config: ...` with `Failed to load config: ...`

**6. lib/graph-api.ts:**
- Line 32: Replace `"Operazione di scrittura non consentita..."` with `"Write operations are not allowed. The Graph Explorer is read-only."`

**7. app/ranking/page.tsx — Variable renames:**
- `type CoalitionFilter = "all" | "maggioranza" | "opposizione"` -> `"all" | "majority" | "opposition"`
- Update all JSX filter values from `"maggioranza"` / `"opposizione"` to `"majority"` / `"opposition"` (these are LOCAL type values, not wire values)
- **IMPORTANT:** Keep `e.coalition === "maggioranza"` comparison strings unchanged — these match the frozen backend SSE wire values. The filter predicate must map: local `"majority"` filter -> comparison `"maggioranza"` on wire data. Update the filter logic accordingly.

**8. types/survey.ts:**
- Translate comment line 299: `// Authority dimensions — Autorita Esperti category` -> `// Authority dimensions — Expert Authority category`

After all changes, run `npx tsc --noEmit` to verify no type errors from the renames. Cascading impact: the BalanceMetrics rename affects ~14 references across 5 files — find and update all of them.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -3 && echo "---" && grep -rn "maggioranzaPercentage\|opposizionePercentage\|commissioni" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "maggioranza_percentage\|opposizione_percentage" | grep -v "type.*commissioni\|commissioni.*:" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "maggioranzaPercentage" frontend/src/ --include="*.ts" --include="*.tsx"` returns 0 matches
    - `grep -rn "opposizionePercentage" frontend/src/ --include="*.ts" --include="*.tsx"` returns 0 matches
    - `grep -rn "formatEventForFrontend\|formatExpert\|formatCitation" frontend/src/app/api/chat/route.ts` returns 0 matches
    - `grep -rn "Metadati opzionali\|Tipi per le API\|Tipi per il sistema\|Configurazione centralizzata\|Operazione di scrittura\|Errore nel caricamento\|Errore durante" frontend/src/ --include="*.ts" --include="*.tsx"` returns 0 matches
    - `npx tsc --noEmit` exits 0
    - User-facing UI strings in JSX remain Italian (spot check: labels, headings unchanged)
  </acceptance_criteria>
  <done>All code-level Italian replaced with English. Dead code removed. Wire-format values preserved. tsc compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Rename routes with redirects and add barrel exports</name>
  <files>
    frontend/src/app/evaluation/page.tsx,
    frontend/src/app/rankings/page.tsx,
    frontend/src/app/explore/page.tsx,
    frontend/src/app/valutazione/page.tsx,
    frontend/src/app/ranking/page.tsx,
    frontend/src/app/explorer/page.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/components/evaluation/index.ts,
    frontend/src/components/graph/index.ts,
    frontend/src/components/search/index.ts,
    frontend/src/components/settings/index.ts,
    frontend/src/components/ui/index.ts
  </files>
  <read_first>
    frontend/src/app/valutazione/page.tsx,
    frontend/src/app/ranking/page.tsx,
    frontend/src/app/explorer/page.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/components/evaluation/EvaluationCharts.tsx,
    frontend/src/components/graph/GraphVisualizer.tsx,
    frontend/src/components/search/DeputySelector.tsx,
    frontend/src/components/search/ResultsList.tsx,
    frontend/src/components/search/ResultDetailDialog.tsx,
    frontend/src/components/search/GroupSelector.tsx,
    frontend/src/components/settings/SettingsModal.tsx,
    frontend/src/components/settings/GraphicalEditors.tsx
  </read_first>
  <action>
**1. Route renames — move page content to new paths:**

Create `frontend/src/app/evaluation/` directory and move the FULL content of `valutazione/page.tsx` to `evaluation/page.tsx`. Then replace `valutazione/page.tsx` with a thin client-side redirect:
```typescript
"use client";
import { useEffect } from "react";
import { useRouter } from "next/navigation";

export default function ValutazioneRedirect() {
  const router = useRouter();
  useEffect(() => {
    router.replace("/evaluation");
  }, [router]);
  return null;
}
```

Create `frontend/src/app/rankings/` directory and move the FULL content of `ranking/page.tsx` to `rankings/page.tsx`. Then replace `ranking/page.tsx` with the same redirect pattern pointing to `/rankings`.

Create `frontend/src/app/explore/` directory and move the FULL content of `explorer/page.tsx` to `explore/page.tsx`. Then replace `explorer/page.tsx` with the same redirect pattern pointing to `/explore`.

**2. Update Sidebar.tsx navigation links:**
- Replace all 4 occurrences of `/ranking` with `/rankings` (2 mobile, 2 desktop — href and pathname checks)
- If there are any `/valutazione` or `/explorer` references (research says there are none), update those too

**3. Check for any other internal navigation:**
- Scan for `<Link href="/ranking"`, `<Link href="/valutazione"`, `<Link href="/explorer"`, `router.push("/ranking")`, etc. across all src/ files
- Update any found references to the new paths

**4. Barrel exports — create index.ts for 5 folders:**

`components/evaluation/index.ts`:
```typescript
export { EvaluationCharts } from "./EvaluationCharts";
```

`components/graph/index.ts`:
```typescript
export { GraphVisualizer } from "./GraphVisualizer";
```

`components/search/index.ts`:
```typescript
export { DeputySelector } from "./DeputySelector";
export { GroupSelector } from "./GroupSelector";
export { ResultsList } from "./ResultsList";
export { ResultDetailDialog } from "./ResultDetailDialog";
```

`components/settings/index.ts`:
```typescript
export { SettingsModal } from "./SettingsModal";
export { GraphicalEditors } from "./GraphicalEditors";
```

`components/ui/index.ts`:
```typescript
// Re-export shadcn/ui primitives — do NOT modify ui/*.tsx source files
export { Button } from "./button";
export { Badge } from "./badge";
export { Card, CardContent, CardHeader, CardTitle } from "./card";
export { Tabs, TabsContent, TabsList, TabsTrigger } from "./tabs";
export { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "./dialog";
export { Popover, PopoverContent, PopoverTrigger } from "./popover";
export { Sheet, SheetContent, SheetTrigger } from "./sheet";
export { ScrollArea } from "./scroll-area";
export { Label } from "./label";
export { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "./tooltip";
export { Alert, AlertDescription } from "./alert";
export { Command, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList } from "./command";
export { Avatar, AvatarFallback, AvatarImage } from "./avatar";
export { Separator } from "./separator";
export { Collapsible, CollapsibleContent, CollapsibleTrigger } from "./collapsible";
export { Textarea } from "./textarea";
export { Input } from "./input";
```

Read each ui/*.tsx file to verify the exact named exports before writing the barrel. The above is approximate — confirm against actual exports.

After all changes, run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -3 && echo "---" && ls -1 src/app/evaluation/page.tsx src/app/rankings/page.tsx src/app/explore/page.tsx src/components/evaluation/index.ts src/components/graph/index.ts src/components/search/index.ts src/components/settings/index.ts src/components/ui/index.ts 2>&1 && echo "---" && grep -c "router.replace" src/app/valutazione/page.tsx src/app/ranking/page.tsx src/app/explorer/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/evaluation/page.tsx` exists with full evaluation page content (not a redirect)
    - `frontend/src/app/rankings/page.tsx` exists with full rankings page content
    - `frontend/src/app/explore/page.tsx` exists with full explore page content
    - `frontend/src/app/valutazione/page.tsx` contains `router.replace("/evaluation")`
    - `frontend/src/app/ranking/page.tsx` contains `router.replace("/rankings")`
    - `frontend/src/app/explorer/page.tsx` contains `router.replace("/explore")`
    - `grep -c "/rankings" frontend/src/components/layout/Sidebar.tsx` returns at least 2
    - `grep -c '"/ranking"' frontend/src/components/layout/Sidebar.tsx` returns 0 (old path gone)
    - All 5 barrel index.ts files exist: evaluation, graph, search, settings, ui
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three routes renamed with client-side redirects. Sidebar links updated. Five barrel exports created. tsc compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `grep -rn ": any\b" frontend/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0 (preserved from Plan 01)
3. Routes /evaluation, /rankings, /explore exist as page files
4. Old routes /valutazione, /ranking, /explorer are redirect stubs
5. No Italian variable names, comments, or docstrings remain in code (UI text stays Italian)
6. `formatEventForFrontend`, `formatExpert`, `formatCitation` deleted
7. All 9 component folders have index.ts barrel exports
</verification>

<success_criteria>
- FE-02: No dead code, barrel exports in all component folders, clean naming
- FE-03: English route paths with redirects from old paths, English variable names and comments throughout
- tsc --noEmit compiles with zero errors
- No behavioral changes to the application
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-02-SUMMARY.md`
</output>
