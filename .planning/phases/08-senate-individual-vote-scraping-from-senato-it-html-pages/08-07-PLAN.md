---
phase: 08-senate-individual-vote-scraping-from-senato-it-html-pages
plan: 07
type: execute
wave: 5
depends_on: ["06"]
files_modified: []
autonomous: false
requirements: [VOT-05, VOT-06]
must_haves:
  truths:
    - "The human explicitly approves launching the long individual ingest before it starts"
    - "Senators have individual vote records after the run (was 0 senators with VOTED)"
    - "The remaining Camera XIX deputies are resumed (was 70 of 391 with VOTED)"
    - "IndividualVotes attach to the correct chamber+legislature Vote nodes (no collision)"
  artifacts:
    - path: ".planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-07-SUMMARY.md"
      provides: "Individual ingest run record with before/after Cypher counts"
      contains: "senators_with_votes"
  key_links:
    - from: "make enrich-votes-individual"
      to: "(Deputy)-[:VOTED]->(IndividualVote)-[:ON_VOTE]->(Vote)"
      via: "Camera resume + Senate per-sitting individual ingest"
      pattern: "VOTED"
---

<objective>
Execute the long-running INDIVIDUAL vote ingest (`make enrich-votes-individual`) against the live Neo4j database, gated behind an explicit human decision (CONTEXT D5). This resumes the remaining ~321 Camera XIX deputies (VOT-06) and ingests all Senate individual votes per-sitting (VOT-05). Depends on Plan 06: the Senate Vote nodes (and Camera 350+ Vote nodes) must already exist for the IndividualVote→ON_VOTE→Vote links to resolve.

Purpose: give both chambers complete per-parliamentarian vote records with the same schema.
Output: IndividualVote nodes + VOTED/ON_VOTE rels for senators and remaining Camera deputies; verified counts; a run record.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-CONTEXT.md
@.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-RESEARCH.md

<preconditions>
Verify (do not assume):
- Plan 06 completed: aggregate Senate Vote nodes exist (`MATCH (:Session {chamber:'senato'})-[:HAS_VOTE]->(v) RETURN count(v)` > 0). Individual votes CANNOT link without them.
- Plan 05 merged: `make enrich-votes-individual` target exists and dry-runs clean.
- Baseline (RESEARCH-verified 2026-07-04): IndividualVote total ~471,609 (from 70 of 391 Camera XIX deputies); 0 senators with VOTED.
- Sanity (RESEARCH Pitfall 6): a senator Deputy.id is the full URI, e.g. `MATCH (d:Deputy {chamber:'senato'}) RETURN d.id LIMIT 3` returns `http://dati.senato.it/senatore/{id}` values.
- Camera XVIII individual votes are a DEFERRED stretch goal (CONTEXT deferred) — do NOT run them here.
</preconditions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve launch of the long individual vote ingest</name>
  <action>Gather the precondition numbers (make -n dry-run, Plan-06 Senate Vote count > 0, baseline individual counts, senator id-format sanity) and present them to the human. Do NOT run Task 2 until the human types "launch-now".</action>
  <decision>Launch `make enrich-votes-individual` (hours-long per-parliamentarian ingest) now?</decision>
  <context>
This runs the long ingest: Camera XIX resume (remaining ~321 deputies, each is many SPARQL pages) + Senate individual votes per-sitting for XIX and XVIII. It is network-bound and HOURS long, but free (no LLM cost). It is resumable (Camera skips deputies with VOTED; Senate skips sittings with individual votes), so it can be re-run safely.

Before approving, the executor MUST report, via `docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "<query>;"`:
- `make -n enrich-votes-individual` output (confirms Camera `--skip-aggregate` + Senate `--individual-only` per legislature).
- Plan-06 gate: `MATCH (:Session {chamber:'senato'})-[:HAS_VOTE]->(v:Vote) RETURN count(v)` → MUST be > 0 (else STOP — run Plan 06 first).
- Baselines: `MATCH (d:Deputy {chamber:'camera'})-[:VOTED]->() RETURN count(DISTINCT d)` (expect ~70); `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->() RETURN count(DISTINCT d)` (expect 0).
- Senator id format: `MATCH (d:Deputy {chamber:'senato'}) RETURN d.id LIMIT 3` → full senatore URIs.
  </context>
  <options>
    <option id="launch-now">
      <name>Launch now</name>
      <pros>Complete per-parliamentarian vote records for both chambers; enables future vote-based features.</pros>
      <cons>Hours-long live run; endpoints may rate-limit; machine busy.</cons>
    </option>
    <option id="defer">
      <name>Defer</name>
      <pros>Run later (e.g. overnight); resumable so no lost progress.</pros>
      <cons>Senators have no individual votes yet; Camera XIX still partial.</cons>
    </option>
  </options>
  <resume-signal>Type "launch-now" to run the ingest, or "defer" to stop here.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run make enrich-votes-individual and verify counts</name>
  <files>(no source files — runs the ingest and verifies the DB)</files>
  <read_first>
    - Makefile (enrich-votes-individual target from Plan 05)
    - 08-RESEARCH.md "Cypher Verification Queries" and Pitfall 6 (senator URI match)
  </read_first>
  <action>
Only run after the human approved "launch-now" AND the Plan-06 Senate Vote count is > 0.

1. Record baselines (Camera deputies with VOTED ~70; senators with VOTED 0; IndividualVote total ~471,609).
2. Launch: `make enrich-votes-individual` (run as a long-running/background command, hours; stream progress logs). This runs Camera XIX resume (`--skip-aggregate --skip-committees --legislature 19`) then Senate individual XIX and XVIII (`--individual-only`).
3. Verify final counts via `docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "<query>;"`:
   - Senators now vote: `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(iv:IndividualVote) RETURN count(DISTINCT d) AS senators_with_votes, count(iv) AS senate_ivotes;` → both > 0 (was 0).
   - Camera resumed: `MATCH (d:Deputy {chamber:'camera'})-[:VOTED]->() RETURN count(DISTINCT d);` → substantially > 70 (approaching 391).
   - Links resolve to right chamber: `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(:IndividualVote)-[:ON_VOTE]->(v:Vote)<-[:HAS_VOTE]-(s:Session) RETURN DISTINCT s.chamber LIMIT 5;` → only 'senato' (no cross-chamber mislink — proves the collision fix).
   - No orphan senator IVotes: `MATCH (iv:IndividualVote) WHERE iv.id STARTS WITH 'iv_senato_' AND NOT (:Deputy)-[:VOTED]->(iv) RETURN count(iv);` → 0 (if > 0, senator URI mismatch per Pitfall 6).
4. Record all before/after numbers in the SUMMARY.

If it fails partway, re-run `make enrich-votes-individual` — it resumes (Camera per-deputy, Senate per-sitting).
  </action>
  <verify>
    <automated>make enrich-votes-individual 2>&1 | tee /tmp/enrich-votes-individual.log; docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(iv:IndividualVote) RETURN count(iv) AS c;" | grep -qE "[1-9][0-9]*" && echo "SENATE INDIVIDUAL VOTES > 0 OK"</automated>
  </verify>
  <done>Senators have individual votes (was 0); remaining Camera XIX deputies resumed; senator IndividualVotes link only to 'senato' Sessions; no orphan senator IndividualVotes.</done>
  <acceptance_criteria>
    - Cypher `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(iv:IndividualVote) RETURN count(DISTINCT d), count(iv)` returns both > 0 (was 0)
    - Cypher `MATCH (d:Deputy {chamber:'camera'})-[:VOTED]->() RETURN count(DISTINCT d)` returns a value > 70
    - Cypher `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(:IndividualVote)-[:ON_VOTE]->(v:Vote)<-[:HAS_VOTE]-(s:Session) RETURN DISTINCT s.chamber` returns only 'senato'
    - Cypher `MATCH (iv:IndividualVote) WHERE iv.id STARTS WITH 'iv_senato_' AND NOT (:Deputy)-[:VOTED]->(iv) RETURN count(iv)` returns 0
    - `make enrich-votes-individual` completed (exit 0) — captured in /tmp/enrich-votes-individual.log
    - SUMMARY records baseline and final counts
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `MATCH (d:Deputy {chamber:'senato'})-[:VOTED]->(iv) RETURN count(DISTINCT d), count(iv)` → both > 0.
- Senator IndividualVotes link only to 'senato' Sessions (collision fix proven).
- No orphan `iv_senato_` IndividualVotes.
</verification>

<success_criteria>
- Human explicitly approved the launch.
- Senate individual votes ingested per-sitting; Camera XIX deputies resumed.
- Links resolve to the correct chamber+legislature Vote nodes; no orphans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-07-SUMMARY.md` with before/after Cypher counts.
</output>
