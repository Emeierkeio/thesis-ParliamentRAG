---
phase: 08-senate-individual-vote-scraping-from-senato-it-html-pages
plan: 06
type: execute
wave: 4
depends_on: ["05"]
files_modified: []
autonomous: false
requirements: [VOT-08]
must_haves:
  truths:
    - "The human explicitly approves launching the aggregate ingest before it starts"
    - "Senate Vote nodes exist after the run (was 0 for both legislatures)"
    - "Camera XIX Vote nodes for sittings > 349 exist after the run (was capped at 349)"
    - "The timeline vote counter is > 0 for a recent Camera sitting and a recent Senate sitting"
  artifacts:
    - path: ".planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-06-SUMMARY.md"
      provides: "Aggregate ingest run record with before/after Cypher counts"
      contains: "senato"
  key_links:
    - from: "make enrich-votes"
      to: "Neo4j Session-[:HAS_VOTE]->Vote for camera 350+ and all senato"
      via: "aggregate ingesters per chamber per legislature"
      pattern: "HAS_VOTE"
---

<objective>
Execute the AGGREGATE vote ingest (`make enrich-votes`) against the live Neo4j database, gated behind an explicit human decision (CONTEXT D5). This populates `Session-[:HAS_VOTE]->Vote` for Camera XIX sittings 350-684, Camera XVIII gap sittings, and ALL Senate sittings (XVIII+XIX) — the Senate currently has 0 Vote nodes. Fulfils VOT-08: the timeline vote counter (which already reads HAS_VOTE) starts showing non-zero counts with zero frontend changes.

Purpose: fix the timeline vote-counter zeros for both chambers and create the Senate Vote nodes that Plan 07's individual votes attach to.
Output: aggregate Vote nodes in Neo4j; verified before/after counts; a run record.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-CONTEXT.md
@.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-RESEARCH.md

<preconditions>
Verify (do not assume):
- Plan 05 merged: `make enrich-votes` target exists and dry-runs clean.
- Neo4j running at bolt://localhost:7689 (container tesi2-neo4j, password thesis2026).
- Baseline (RESEARCH-verified 2026-07-04): Vote nodes total 17,473; Camera XIX (≤349) 7,355; Camera XVIII 10,118; Senato 0.
- Network reachable: dati.camera.it (POST) and dati.senato.it (GET, browser UA).
</preconditions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve launch of the aggregate vote ingest</name>
  <action>Gather the precondition numbers (make -n dry-run of enrich-votes, baseline Cypher counts below) and present them to the human. Do NOT run Task 2 until the human types "launch-now".</action>
  <decision>Launch `make enrich-votes` (aggregate vote ingest) now?</decision>
  <context>
This runs the aggregate ingesters for both chambers and both legislatures. It is network-bound (minutes-to-tens-of-minutes, no LLM cost, free). It creates new Vote nodes and Session-[:HAS_VOTE]->Vote edges: Camera XIX 350-684, Camera XVIII gaps, and all Senate sittings. Existing Camera XIX sittings <=349 are skipped (they already have XML-sourced votes), so timeline counts are not doubled.

Before approving, the executor MUST report:
- `make -n enrich-votes` output (confirms the four ingester invocations).
- Baseline Cypher counts (run each and record), via `docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "<query>;"`:
  - `MATCH (v:Vote) RETURN count(v)` (expect ~17,473)
  - `MATCH (s:Session {chamber:'senato'})-[:HAS_VOTE]->(v:Vote) RETURN count(v)` (expect 0)
  - `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v:Vote) WHERE s.number > 349 RETURN count(v)` (expect 0)
  </context>
  <options>
    <option id="launch-now">
      <name>Launch now</name>
      <pros>Timeline vote counters populate for both chambers; Senate Vote nodes ready for Plan 07 individual votes.</pros>
      <cons>Tens of minutes of live SPARQL; endpoints may rate-limit.</cons>
    </option>
    <option id="defer">
      <name>Defer</name>
      <pros>Run later at a convenient time.</pros>
      <cons>Timeline still shows zero votes; Plan 07 blocked (no Senate Vote nodes to attach to).</cons>
    </option>
  </options>
  <resume-signal>Type "launch-now" to run the ingest, or "defer" to stop here.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run make enrich-votes and verify aggregate counts</name>
  <files>(no source files — runs the ingest and verifies the DB)</files>
  <read_first>
    - Makefile (enrich-votes target from Plan 05)
    - 08-RESEARCH.md "Cypher Verification Queries" and "Neo4j Current State"
  </read_first>
  <action>
Only run after the human approved "launch-now".

1. Record baseline counts (the three Cypher queries from Task 1).
2. Launch: `make enrich-votes` (run as a long-running/background command; stream progress). It ingests Camera XIX 350+, Camera XVIII gaps, Senate XIX, Senate XVIII aggregates.
3. Verify final counts via `docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "<query>;"`:
   - Senate now has votes: `MATCH (s:Session {chamber:'senato', legislature:19})-[:HAS_VOTE]->(v:Vote) RETURN count(DISTINCT s) AS sen19_sessions, count(v) AS sen19_votes;` → both > 0 (was 0).
   - `MATCH (s:Session {chamber:'senato', legislature:18})-[:HAS_VOTE]->(v:Vote) RETURN count(v);` → > 0.
   - Camera post-349 now has votes: `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v:Vote) WHERE s.number > 349 RETURN count(v);` → > 0.
   - Total votes increased: `MATCH (v:Vote) RETURN count(v);` → > 17,473.
   - No doubling on old sittings: `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v:Vote) WHERE s.number <= 349 RETURN count(v);` → still ~7,355 (unchanged).
4. Timeline spot check (VOT-08): confirm a recent Camera sitting has votes, e.g. `MATCH (s:Session {chamber:'camera', legislature:19, number:405})-[:HAS_VOTE]->(v) RETURN count(v);` → > 0; and one recent Senate sitting > 0.
5. Record all before/after numbers in the SUMMARY.

If the run fails partway, it is idempotent/resumable (skip-if-HAS_VOTE per sitting) — re-run `make enrich-votes`.
  </action>
  <verify>
    <automated>make enrich-votes 2>&1 | tee /tmp/enrich-votes.log; docker exec tesi2-neo4j cypher-shell -u neo4j -p thesis2026 "MATCH (:Session {chamber:'senato'})-[:HAS_VOTE]->(v:Vote) RETURN count(v) AS c;" | grep -qE "[1-9][0-9]*" && echo "SENATE VOTES > 0 OK"</automated>
  </verify>
  <done>Senate Vote nodes exist (was 0); Camera XIX votes for sittings > 349 exist; total Vote count increased; Camera <=349 counts unchanged (no doubling); a recent Camera and Senate sitting each show voteCount > 0.</done>
  <acceptance_criteria>
    - Cypher `MATCH (:Session {chamber:'senato'})-[:HAS_VOTE]->(v:Vote) RETURN count(v)` returns > 0 (was 0)
    - Cypher `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v:Vote) WHERE s.number > 349 RETURN count(v)` returns > 0
    - Cypher `MATCH (v:Vote) RETURN count(v)` returns a value > 17,473
    - Cypher `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v:Vote) WHERE s.number <= 349 RETURN count(v)` is unchanged (~7,355)
    - `make enrich-votes` completed (exit 0) — captured in /tmp/enrich-votes.log
    - SUMMARY records baseline and final counts
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `MATCH (:Session {chamber:'senato'})-[:HAS_VOTE]->(v) RETURN count(v)` → > 0.
- `MATCH (s:Session {chamber:'camera', legislature:19})-[:HAS_VOTE]->(v) WHERE s.number > 349 RETURN count(v)` → > 0.
- Camera <=349 vote count unchanged (no doubling).
</verification>

<success_criteria>
- Human explicitly approved the launch.
- Aggregate Vote nodes ingested for Camera 350+ and all Senate; timeline counters non-zero.
- No duplicate votes on pre-existing Camera sittings.
</success_criteria>

<output>
After completion, create `.planning/phases/08-senate-individual-vote-scraping-from-senato-it-html-pages/08-06-SUMMARY.md` with before/after Cypher counts.
</output>
