---
phase: 02-backend
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/app/services/experts.py
  - backend/app/services/deps.py
  - backend/app/routers/chat.py
  - backend/app/routers/query.py
  - backend/scripts/seed_evaluation_topic.py
  - backend/tests/unit/test_experts.py
  - backend/tests/unit/test_import_violations.py
autonomous: true
requirements:
  - SVC-02
  - API-02
  - SCR-02

must_haves:
  truths:
    - "Expert computation exists in exactly one place: services/experts.py"
    - "chat.py and query.py both delegate expert computation to the service"
    - "seed_evaluation_topic.py imports from services, not from routers"
    - "evaluation.py does not import from survey.py"
  artifacts:
    - path: "backend/app/services/experts.py"
      provides: "Unified expert computation service"
      contains: "compute_experts"
      min_lines: 50
    - path: "backend/tests/unit/test_experts.py"
      provides: "Tests for expert computation logic"
      contains: "test_compute_experts"
    - path: "backend/tests/unit/test_import_violations.py"
      provides: "Tests preventing cross-layer imports"
      contains: "test_no_router_imports_from_router"
  key_links:
    - from: "backend/app/routers/chat.py"
      to: "backend/app/services/experts.py"
      via: "import compute_experts"
      pattern: "from.*services.*experts.*import"
    - from: "backend/scripts/seed_evaluation_topic.py"
      to: "backend/app/services/experts.py"
      via: "import compute_experts"
      pattern: "from.*services.*experts.*import"
---

<objective>
Extract the duplicated expert computation into a unified `services/experts.py` module and fix all cross-layer import violations.

Purpose: Expert computation is duplicated between chat.py (~88 lines) and query.py (~70 lines) with subtle differences. The unified service uses the chat.py formula (0.70*authority + 0.30*similarity) as the single implementation per locked decision. This also fixes API-02 (cross-router imports) and SCR-02 (script-to-router import).

Output: New experts.py service, both routers delegating to it, import violation tests, coupling fixes.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend/02-CONTEXT.md
@.planning/phases/02-backend/02-RESEARCH.md

<interfaces>
<!-- Expert computation diff from research -->
chat.py::_compute_experts_for_frontend (line 1355):
- Ranking: 0.70 * authority_score + 0.30 * best_chunk_similarity
- Tracks best_chunk_similarity per speaker
- Handles party_changed -> current_party
- Has _patch_experts_for_cited_speakers as separate function

query.py::_compute_experts (line 594):
- Ranking: authority_score only (no similarity component)
- No best_similarity tracking
- No party_changed handling
- Inline post-gen patch in process_query_streaming

Expert dict shape (FROZEN):
```json
{
  "id": "speaker_id_string",
  "first_name": "Mario", "last_name": "Rossi",
  "group": "Partito Democratico - ...",
  "coalition": "opposizione",
  "authority_score": 0.72,
  "relevant_speeches_count": 5,
  "camera_profile_url": "https://...",
  "photo": "https://...",
  "profession": "Avvocato",
  "education": "Laurea in Giurisprudenza",
  "committee": "Commissione Giustizia",
  "institutional_role": null,
  "score_breakdown": {...}
}
```

<!-- DI from Plan 02 -->
From backend/app/services/deps.py (after Plan 02):
```python
@lru_cache()
def get_neo4j_client() -> Neo4jClient: ...
```

<!-- Cross-router violations -->
evaluation.py line 29: from app.routers.survey import _load_surveys, _calculate_stats
seed_evaluation_topic.py line 34: from app.routers.chat import _compute_experts_for_frontend
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unified experts.py service with tests</name>
  <files>
    backend/app/services/experts.py,
    backend/app/services/deps.py,
    backend/tests/unit/test_experts.py
  </files>
  <read_first>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/services/deps.py,
    backend/app/models/evidence.py,
    backend/app/services/neo4j_client.py
  </read_first>
  <behavior>
    - Test 1: compute_experts returns one expert per party (given 3 speakers from 2 parties, returns 2 experts)
    - Test 2: Ranking uses combined formula by default (0.70 * authority + 0.30 * similarity)
    - Test 3: party_changed speakers use current_party not original party
    - Test 4: Expert dict contains all frozen fields (id, first_name, last_name, group, coalition, authority_score, relevant_speeches_count, score_breakdown)
    - Test 5: GovernmentMember speakers are excluded from expert list
    - Test 6: patch_experts_for_cited_speakers updates experts based on actual citations
  </behavior>
  <action>
1. Write tests FIRST in `backend/tests/unit/test_experts.py`:
   - Create fixtures for mock evidence lists, authority_scores dicts, authority_details dicts
   - Test the expected behaviors listed above using mock data
   - Mock `_fetch_speaker_details` to return a canned dict (no real DB calls)

2. Create `backend/app/services/experts.py` using chat.py's `_compute_experts_for_frontend` as the base implementation:
   - Extract the function body VERBATIM from chat.py first
   - Signature:
     ```python
     async def compute_experts(
         evidence_list: list,
         authority_scores: dict[str, float],
         authority_details: dict[str, dict],
         neo4j_client: Neo4jClient,
         ranking_formula: str = "combined",
     ) -> list[dict]:
     ```
   - The `ranking_formula` param supports `"combined"` (0.70*auth + 0.30*sim, default) and `"authority_only"` (authority_score alone)
   - Extract `_fetch_speaker_details()` helper into the service module
   - Extract `_patch_experts_for_cited_speakers()` as a separate public function:
     ```python
     async def patch_experts_for_cited_speakers(
         experts: list[dict],
         verified_citations: list,
         authority_scores: dict[str, float],
         authority_details: dict[str, dict],
         neo4j_client: Neo4jClient,
     ) -> list[dict]:
     ```

3. Add `get_expert_service` to `deps.py` — but since experts.py contains functions (not a class), just export the functions directly. No Depends wrapper needed for a function module.

4. Run tests, make them pass.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/test_experts.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/experts.py` exists and contains `async def compute_experts(`
    - `backend/app/services/experts.py` contains `0.70` and `0.30` (combined formula)
    - `backend/app/services/experts.py` contains `party_changed` handling
    - `backend/tests/unit/test_experts.py` has at least 5 test functions
    - `cd backend && python -m pytest tests/unit/test_experts.py -x -q` passes all tests
  </acceptance_criteria>
  <done>Unified experts.py service created with combined ranking formula, party_changed handling, and patch function. All expert tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routers and scripts to use experts service, fix all import violations</name>
  <files>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/routers/evaluation.py,
    backend/app/routers/survey.py,
    backend/scripts/seed_evaluation_topic.py,
    backend/tests/unit/test_import_violations.py
  </files>
  <read_first>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/routers/evaluation.py,
    backend/app/routers/survey.py,
    backend/scripts/seed_evaluation_topic.py,
    backend/app/services/experts.py
  </read_first>
  <action>
**chat.py:**
1. Remove `_compute_experts_for_frontend()` function entirely
2. Remove `_patch_experts_for_cited_speakers()` function entirely
3. Remove `_fetch_speaker_details()` if it was only used by the above
4. Add import: `from app.services.experts import compute_experts, patch_experts_for_cited_speakers`
5. Replace all calls to `_compute_experts_for_frontend(...)` with `await compute_experts(evidence_list, authority_scores, authority_details, neo4j_client)`
6. Replace all calls to `_patch_experts_for_cited_speakers(...)` with `await patch_experts_for_cited_speakers(experts, verified_citations, authority_scores, authority_details, neo4j_client)`
7. Do NOT change any SSE event names, payload shapes, or emission order

**query.py:**
1. Remove `_compute_experts()` function entirely
2. Remove inline post-generation expert patch code (lines ~422-502)
3. Add import: `from app.services.experts import compute_experts, patch_experts_for_cited_speakers`
4. Replace expert computation call with `await compute_experts(evidence_list, authority_scores, authority_details, neo4j_client)`
5. Replace inline expert patch with `await patch_experts_for_cited_speakers(...)`
6. Do NOT change SSE events

**evaluation.py (fix API-02 violation):**
1. Find the line: `from app.routers.survey import _load_surveys, _calculate_stats`
2. Move `_load_surveys` and `_calculate_stats` from `survey.py` to a shared location. Two options:
   a. Move to `services/evaluation_service.py` (will be created in Plan 04)
   b. Move to a temporary `services/survey_helpers.py` for now
   Choose option (b) — create `backend/app/services/survey_helpers.py` with the extracted functions
3. Update evaluation.py to import from `app.services.survey_helpers`
4. Update survey.py to import from `app.services.survey_helpers` (instead of defining locally)

**seed_evaluation_topic.py (fix SCR-02 violation):**
1. Find the line: `from app.routers.chat import _compute_experts_for_frontend`
2. Replace with: `from app.services.experts import compute_experts`
3. Update the call site to use the new function signature

**test_import_violations.py:**
Create `backend/tests/unit/test_import_violations.py` with:
- `test_no_router_imports_from_router`: Read source of evaluation.py, assert `"from app.routers.survey"` not in source and `"from app.routers"` not in source (except its own imports)
- `test_no_script_imports_from_router`: Read source of seed_evaluation_topic.py, assert `"from app.routers"` not in source
- `test_search_no_local_client`: Assert `"_neo4j_client"` not in search.py source (verify Plan 02 fix still holds)
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG && grep -n "from app.routers.survey" backend/app/routers/evaluation.py; grep -n "from app.routers.chat" backend/scripts/seed_evaluation_topic.py; echo "---"; cd backend && python -m pytest tests/unit/test_import_violations.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from app.routers.survey" backend/app/routers/evaluation.py` returns zero matches
    - `grep "from app.routers.chat" backend/scripts/seed_evaluation_topic.py` returns zero matches
    - `grep "_compute_experts_for_frontend" backend/app/routers/chat.py` returns zero matches
    - `grep "_compute_experts" backend/app/routers/query.py` returns zero matches (no local expert function)
    - `grep "from app.services.experts import" backend/app/routers/chat.py` returns 1 match
    - `cd backend && python -m pytest tests/unit/test_import_violations.py -x -q` passes
  </acceptance_criteria>
  <done>Expert computation unified in services/experts.py. Both routers delegate to the service. All cross-layer import violations fixed: evaluation.py no longer imports from survey.py, seed script no longer imports from chat.py. Import violation tests enforce this.</done>
</task>

</tasks>

<verification>
- `grep -rn "_compute_experts_for_frontend\|_compute_experts" backend/app/routers/` returns zero matches for local definitions
- `grep -rn "from app.routers" backend/app/routers/evaluation.py backend/scripts/seed_evaluation_topic.py` shows no cross-layer imports
- `cd backend && python -m pytest tests/unit/ -x -q` all pass
- `backend/app/services/experts.py` contains the unified implementation
</verification>

<success_criteria>
1. Expert computation exists in exactly one place (services/experts.py)
2. Both chat.py and query.py delegate to the service
3. Zero cross-router import violations
4. seed_evaluation_topic.py imports from services only
5. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend/02-03-SUMMARY.md`
</output>
