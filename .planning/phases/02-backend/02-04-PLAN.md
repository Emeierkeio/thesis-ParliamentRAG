---
phase: 02-backend
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - backend/app/services/evaluation_service.py
  - backend/app/routers/evaluation.py
  - backend/tests/unit/test_evaluation_service.py
autonomous: true
requirements:
  - SVC-03
  - API-01

must_haves:
  truths:
    - "Evaluation business logic lives in services/evaluation_service.py, not in the router"
    - "evaluation.py router contains only HTTP orchestration (input validation, service call, response formatting)"
    - "Historical bug fixes from MEMORY.md are preserved exactly in the service"
  artifacts:
    - path: "backend/app/services/evaluation_service.py"
      provides: "Evaluation metric computation service"
      contains: "_compute_automated_metrics"
      min_lines: 200
    - path: "backend/tests/unit/test_evaluation_service.py"
      provides: "Tests for evaluation metric computation"
      contains: "test_"
  key_links:
    - from: "backend/app/routers/evaluation.py"
      to: "backend/app/services/evaluation_service.py"
      via: "import from service"
      pattern: "from.*services.*evaluation_service.*import"
---

<objective>
Extract evaluation business logic from the 959-line evaluation.py router into a dedicated evaluation_service.py module.

Purpose: evaluation.py is the riskiest refactoring target. It contains ~400 lines of metric computation with three documented historical bug fixes (MEMORY.md). The extraction must be surgical: move function bodies verbatim, add tests that encode the known correct behavior, then clean up. The router becomes a thin HTTP wrapper.

This plan runs after Plan 03 (Wave 2) which extracts survey_helpers.py, so the cross-router import from evaluation.py to survey.py is already resolved before Task 2 thins the router.

Output: New evaluation_service.py with all metric computation, thin evaluation.py router, comprehensive tests.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend/02-CONTEXT.md
@.planning/phases/02-backend/02-RESEARCH.md

<interfaces>
<!-- Historical bug fixes that MUST be preserved (from MEMORY.md) -->

Bug fix #1 (baseline authority inflation):
- _compute_baseline_authority_from_precomputed() uses pre-computed baseline_experts
- Per-chat: _compute_automated_metrics accepts baseline_precomputed_experts param
- Aggregate: get_dashboard iterates eval_set_raw using precomputed experts first
- Fallback: still uses expert_full_lookup if no precomputed experts

Bug fix #2 (detail view inconsistency):
- Replaced expert_full_lookup fallback with party_top_expert (stored top expert per party from current chat)
- Both detail and A/B panel use same expert per cited group

Bug fix #3 (experts panel showed wrong parties):
- Post-generation expert update: cited_party_best maps party -> highest-authority cited speaker
- Keep existing expert if their id matches; otherwise fetch new details
- Second experts SSE event replaces the first

<!-- evaluation.py structure -->
evaluation.py (959 lines):
- Router endpoints: get_dashboard(), get_evaluation_detail(), etc.
- Business logic: _compute_automated_metrics(), _compute_baseline_authority_full(),
  _compute_baseline_authority_from_precomputed(), aggregation helpers
- Import violation: from app.routers.survey import _load_surveys, _calculate_stats
  (fixed by Plan 03's survey_helpers.py extraction — Plan 03 runs before this plan)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract evaluation service and write tests</name>
  <files>
    backend/app/services/evaluation_service.py,
    backend/tests/unit/test_evaluation_service.py
  </files>
  <read_first>
    backend/app/routers/evaluation.py,
    backend/app/services/neo4j_client.py,
    backend/app/models/evidence.py
  </read_first>
  <behavior>
    - Test 1: _compute_automated_metrics returns expected metric keys (authority_by_group, citation_quality, etc.)
    - Test 2: _compute_baseline_authority_from_precomputed uses precomputed experts (not expert_full_lookup)
    - Test 3: party_top_expert fallback is used for cited deputies not in chatData.experts (bug fix #2)
    - Test 4: Metric values are float type and in [0,1] range where applicable
    - Test 5: Empty evidence list returns graceful defaults (not crash)
    - Test 6: aggregate_metrics averages correctly over multiple chats
  </behavior>
  <action>
READ evaluation.py COMPLETELY (all 959 lines) before writing any code. Identify:
- All private functions that compute metrics (the "business logic")
- All functions that are pure HTTP handlers (the "router" part)
- All imports, especially any that depend on FastAPI request/response

**Step 1: Write tests first** in `backend/tests/unit/test_evaluation_service.py`:
- Create mock data fixtures that represent ChatHistory data as stored in Neo4j
- Create mock evaluation_set.json data with baseline_experts
- Test each behavior listed above
- Use explicit expected values (not just "is not None")

**Step 2: Create `backend/app/services/evaluation_service.py`:**
- Extract VERBATIM all metric computation functions from evaluation.py:
  - `_compute_automated_metrics()` — the main per-chat metric computation
  - `_compute_baseline_authority_full()` — fallback baseline computation
  - `_compute_baseline_authority_from_precomputed()` — preferred baseline computation
  - Any helper functions these call (party normalization, score aggregation, etc.)
- Keep function signatures identical to current code
- Add type hints where missing
- Add English docstrings
- **CRITICAL**: Do NOT modify any computation logic. The historical bug fixes in these functions are correct and must be preserved. Only move code location, not code behavior.
- If functions reference `evaluation_set.json`, accept the data as a parameter (don't hard-code file paths in the service)

**Step 3: Run tests, verify they pass.**

Note: The `_load_surveys` / `_calculate_stats` functions that evaluation.py imports from survey.py are now in survey_helpers.py (done by Plan 03). Update the import in evaluation_service.py to use the shared helper module, not the survey router.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/test_evaluation_service.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/evaluation_service.py` exists and contains `def _compute_automated_metrics(`
    - `backend/app/services/evaluation_service.py` contains `_compute_baseline_authority_from_precomputed`
    - `backend/app/services/evaluation_service.py` contains `party_top_expert` (bug fix #2 preserved)
    - `grep "from app.routers" backend/app/services/evaluation_service.py` returns zero matches
    - `backend/tests/unit/test_evaluation_service.py` has at least 6 test functions
    - All tests pass
  </acceptance_criteria>
  <done>Evaluation service extracted with all metric computation functions. Historical bug fixes verified preserved via tests. Service has zero router imports.</done>
</task>

<task type="auto">
  <name>Task 2: Thin evaluation.py router to use service</name>
  <files>
    backend/app/routers/evaluation.py
  </files>
  <read_first>
    backend/app/routers/evaluation.py,
    backend/app/services/evaluation_service.py
  </read_first>
  <action>
1. Add import: `from app.services.evaluation_service import (list of extracted functions)`
2. Remove all function bodies that were extracted to the service
3. Replace with calls to the service functions
4. Keep all `@router.get(...)` / `@router.post(...)` decorators and their HTTP-level logic (request parsing, response formatting, error handling)
5. The router functions should follow the thin pattern:
   ```python
   @router.get("/dashboard")
   async def get_dashboard(...):
       # Input validation
       # Call service
       result = compute_dashboard_metrics(neo4j_client, eval_set_data, ...)
       # Format and return response
       return result
   ```
6. Remove any now-unused imports (no dead imports)
7. Verify the router still has all its original endpoints — do NOT remove any endpoint

**Size check after extraction:** The router should be roughly 400-500 lines (down from 959), containing only endpoint definitions, request parsing, and response formatting.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && wc -l app/routers/evaluation.py; echo "---"; python -c "from app.routers.evaluation import router; print(f'Endpoints: {len(router.routes)}')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l backend/app/routers/evaluation.py` shows fewer than 600 lines (down from 959)
    - `grep "def _compute_automated_metrics" backend/app/routers/evaluation.py` returns zero matches (moved to service)
    - `grep "from app.services.evaluation_service import" backend/app/routers/evaluation.py` returns 1 match
    - All original router endpoints still exist (same count)
    - `cd backend && python -m pytest tests/unit/ -x -q` all pass (including evaluation_service tests)
  </acceptance_criteria>
  <done>evaluation.py router thinned to HTTP orchestration only. All metric computation delegated to evaluation_service.py. All endpoints preserved. Line count reduced by ~40%.</done>
</task>

</tasks>

<verification>
- `wc -l backend/app/routers/evaluation.py` < 600 lines
- `wc -l backend/app/services/evaluation_service.py` > 200 lines
- `grep -c "def _compute" backend/app/routers/evaluation.py` returns 0
- `cd backend && python -m pytest tests/unit/test_evaluation_service.py -x -q` all pass
</verification>

<success_criteria>
1. Evaluation business logic in services/evaluation_service.py
2. evaluation.py is a thin router (~400-500 lines)
3. Historical bug fixes preserved (verified by tests)
4. No business logic in router functions
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend/02-04-SUMMARY.md`
</output>
