---
phase: 02-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/unit/__init__.py
  - backend/tests/integration/__init__.py
  - backend/docs/SSE_CONTRACT.md
  - backend/app/services/neo4j_client.py
  - backend/app/services/retrieval/graph_channel.py
  - backend/app/routers/evidence.py
  - backend/app/routers/query.py
  - backend/app/routers/chat.py
  - backend/app/routers/search.py
  - backend/app/models/evidence.py
  - backend/app/services/authority/scorer.py
  - backend/tests/unit/test_cypher_queries.py
autonomous: true
requirements:
  - SVC-01
  - API-03
  - QA-01

must_haves:
  truths:
    - "No Cypher query in the codebase references start_char_raw or end_char_raw"
    - "No Pydantic model contains span_start or span_end fields"
    - "SSE contract document lists all event types with payload shapes"
    - "Test infrastructure exists and pytest collects tests"
  artifacts:
    - path: "backend/docs/SSE_CONTRACT.md"
      provides: "Frozen SSE event contract documentation"
      contains: "chunk"
    - path: "backend/tests/conftest.py"
      provides: "Shared test fixtures with mock Neo4j"
      contains: "mock_neo4j"
    - path: "backend/tests/unit/test_cypher_queries.py"
      provides: "Tests verifying dead properties are removed"
      contains: "start_char_raw"
  key_links:
    - from: "backend/app/services/neo4j_client.py"
      to: "Neo4j database"
      via: "Cypher queries without dead properties"
      pattern: "RETURN c\\.id"
---

<objective>
Create test infrastructure, document the frozen SSE contract, and remove all dead Cypher properties (start_char_raw, end_char_raw, span_start, span_end) from the entire backend — including scorer.py and search.py.

Purpose: This is the foundational plan that makes the codebase deployable against the Phase 1 Neo4j schema. Without removing dead properties, queries return null. The SSE contract document must exist BEFORE any router refactoring (locked decision). Test infrastructure enables all subsequent plans to have automated verification.

Output: Test scaffold in backend/tests/, SSE_CONTRACT.md, and all Cypher queries updated.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend/02-CONTEXT.md
@.planning/phases/02-backend/02-RESEARCH.md

<interfaces>
<!-- Current deps.py pattern (being replaced in Plan 02, but referenced here for test fixtures) -->
From backend/app/services/deps.py:
```python
def get_services() -> dict:
    # Returns {"neo4j": Neo4jClient, "retrieval": RetrievalEngine, ...}
```

From backend/app/models/evidence.py:
```python
class UnifiedEvidence(BaseModel):
    # Has span_start: int and span_end: int fields TO BE REMOVED
```

From backend/app/routers/query.py:
```python
class CitationInfo(BaseModel):
    # Has span_start: int and span_end: int fields TO BE REMOVED (lines 88-89)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and SSE contract document</name>
  <files>
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/unit/__init__.py,
    backend/tests/integration/__init__.py,
    backend/tests/unit/test_cypher_queries.py,
    backend/docs/SSE_CONTRACT.md
  </files>
  <read_first>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/services/deps.py,
    backend/app/services/neo4j_client.py,
    backend/app/main.py
  </read_first>
  <action>
1. Check pytest/httpx are installed: `pip show pytest pytest-asyncio httpx`. Install if missing.

2. Create `backend/tests/__init__.py` (empty), `backend/tests/unit/__init__.py` (empty), `backend/tests/integration/__init__.py` (empty).

3. Create `backend/tests/conftest.py` with:
   - `import pytest` and `from unittest.mock import MagicMock`
   - `from fastapi.testclient import TestClient`
   - `from app.main import app`
   - A `mock_neo4j` fixture returning a MagicMock with `query` returning `[]`, `query_single` returning `None`, `vector_search` returning `[]`, and `close` as a no-op
   - A `client` fixture that sets `app.dependency_overrides[get_neo4j_client] = lambda: mock_neo4j` (import `get_neo4j_client` from `app.services.deps` — NOTE: this import will work after Plan 02 rewrites deps.py; for now, use `from app.services.deps import get_services` and override that instead, or just mock at module level). Since deps.py is being rewritten in Plan 02, use a simpler approach: mock at the module attribute level with `@patch("app.services.deps._neo4j_client", mock_neo4j)`. The fixture should `yield TestClient(app)` and clean up after.
   - A `sample_evidence` fixture returning a dict with keys: `chunk_id`, `chunk_text`, `speech_id`, `text`, `speaker_id`, `speaker_first_name`, `speaker_last_name`, `speaker_type`, `session_id`, `session_date`, `session_number`, `debate_title`, `score` — NO `span_start` or `span_end`.

4. Create `backend/tests/unit/test_cypher_queries.py` with tests that:
   - `test_neo4j_client_vector_search_no_dead_properties`: Read `neo4j_client.py` source, assert `"start_char_raw"` not in the file content and `"end_char_raw"` not in the file content
   - `test_graph_channel_no_dead_properties`: Same for `graph_channel.py`
   - `test_evidence_router_no_dead_properties`: Same for `routers/evidence.py`
   - `test_query_router_no_dead_properties`: Same for `routers/query.py`
   - `test_chat_router_no_dead_properties`: Same for `routers/chat.py`
   - `test_search_router_no_dead_properties`: Same for `routers/search.py`
   - `test_scorer_no_dead_properties`: Same for `services/authority/scorer.py`
   - `test_unified_evidence_no_span_fields`: Import `UnifiedEvidence` from `app.models.evidence`, assert `"span_start"` not in `UnifiedEvidence.model_fields` and `"span_end"` not in `UnifiedEvidence.model_fields`
   - Implementation: use `inspect.getsource()` or `pathlib.Path.read_text()` to read source files

5. Create `backend/docs/SSE_CONTRACT.md` documenting the frozen SSE event contract. Use the exact tables from the research (02-RESEARCH.md, SSE Event Contract section). Include:
   - Header explaining this is a FROZEN contract
   - chat.py event table (20 events including waiting, progress x7, commissioni, experts x2, citations, balance, compass, topic_stats, chunk, citation_details, hq_variants, complete, error)
   - query.py event table (15 events)
   - Expert dict shape (frozen JSON example)
   - Note about chunk payload key difference: `content:` in chat.py vs `data:` in query.py
   - Note about dual experts emission being intentional
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/ --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/conftest.py` exists and contains `mock_neo4j`
    - `backend/tests/unit/test_cypher_queries.py` exists and contains `test_neo4j_client_vector_search_no_dead_properties`
    - `backend/docs/SSE_CONTRACT.md` exists and contains `FROZEN`
    - `pytest --collect-only` finds at least 8 test items (added scorer + search tests)
  </acceptance_criteria>
  <done>Test infrastructure created with conftest.py, 8+ dead-property tests in test_cypher_queries.py (including scorer.py and search.py), and SSE_CONTRACT.md fully documenting both pipeline event sequences</done>
</task>

<task type="auto">
  <name>Task 2: Remove all dead Cypher properties and Pydantic span fields</name>
  <files>
    backend/app/services/neo4j_client.py,
    backend/app/services/retrieval/graph_channel.py,
    backend/app/routers/evidence.py,
    backend/app/routers/query.py,
    backend/app/routers/chat.py,
    backend/app/routers/search.py,
    backend/app/models/evidence.py,
    backend/app/services/authority/scorer.py
  </files>
  <read_first>
    backend/app/services/neo4j_client.py,
    backend/app/services/retrieval/graph_channel.py,
    backend/app/routers/evidence.py,
    backend/app/routers/query.py,
    backend/app/routers/chat.py,
    backend/app/routers/search.py,
    backend/app/models/evidence.py,
    backend/app/services/authority/scorer.py
  </read_first>
  <action>
Follow the exact dead-property hit map from 02-RESEARCH.md. For each file:

**neo4j_client.py** (lines ~159-160):
- In `vector_search()` RETURN clause, remove the lines `c.start_char_raw AS span_start` and `c.end_char_raw AS span_end`
- Remove `span_start` and `span_end` from any dict construction using the query results
- ADD to the RETURN clause: `i.speakingRole AS speaking_role` and `f.phaseType AS phase_type` (expose new Phase 1 schema data per locked decision)

**graph_channel.py** (lines ~285-286, 336, 341, 396-397):
- In `_get_chunks_from_signatories()` RETURN clause, remove `c.start_char_raw AS span_start` and `c.end_char_raw AS span_end`
- Remove the comment on line ~341 referencing `start_char_raw/end_char_raw`
- In `_process_results()`, remove `span_start = row.get("span_start", 0)` and the corresponding span_end line
- Remove `span_start` and `span_end` from any processed dict construction (lines ~396-397)

**evidence.py router** (lines ~97-98, 187-188):
- In `get_evidence()` Cypher, remove `c.start_char_raw AS span_start` and `c.end_char_raw AS span_end`
- In `verify_evidence()` Cypher, same removal
- Remove span_start/span_end from any response dict construction
- UPDATE: After removing span fields, the `compute_quote_text` logic that uses `span_start=0, span_end=0` will fall back to `chunk_text`. Remove the `[:200]` truncation on the fallback — use full `chunk_text` as `quote_text` (per research recommendation)

**query.py** (lines ~88-89, 284-285, 339-340, 394-397):
- Remove `span_start: int` and `span_end: int` from `CitationInfo` Pydantic model
- In the extra citation DB lookup Cypher (~line 284), remove `c.start_char_raw AS span_start` and `c.end_char_raw AS span_end`
- Remove `"span_start": row.get(...)` and `"span_end": row.get(...)` from `extra_evidence_map` construction (~lines 339-340)
- Remove `"span_start": ev.get(...)` and `"span_end": ev.get(...)` from citation recovery section (~lines 394-397)

**chat.py** (lines ~427-428, 475-476, 910-911, 958-959, 1028-1030):
- In `process_chat_background`: remove `c.start_char_raw AS span_start` / `c.end_char_raw AS span_end` from citation lookup Cypher (~lines 427-428), and `"span_start": row.get(...)` from dict construction (~lines 475-476)
- In `process_chat_streaming`: same pattern at ~lines 910-911 (Cypher) and 958-959 (dict construction)
- In citation recovery from text: remove `"span_start": ev.get(...)` lines (~1028-1030)

**search.py**:
- Read the full file and identify any Cypher RETURN clauses that reference `c.start_char_raw AS span_start` or `c.end_char_raw AS span_end`
- Remove all such occurrences from Cypher strings and from any dict construction using those returned values
- Verify no `span_start` or `span_end` remain anywhere in the file

**scorer.py** (backend/app/services/authority/scorer.py):
- Read the full file and identify any Cypher RETURN clauses that reference `c.start_char_raw AS span_start` or `c.end_char_raw AS span_end`
- Remove all such occurrences from Cypher strings and from any dict construction using those returned values
- Verify no `start_char_raw`, `end_char_raw`, `span_start`, or `span_end` remain anywhere in the file

**models/evidence.py**:
- Remove `span_start: int` and `span_end: int` fields from `UnifiedEvidence` model
- Remove `span_start` and `span_end` from any `EvidenceResponse` model if present
- Remove any `span_start`/`span_end` from other Pydantic models in the file

Do NOT change any SSE event names, payload field names, or emission order. Do NOT change any other Cypher property names — only remove the dead ones listed above.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG && grep -rn "start_char_raw\|end_char_raw\|span_start\|span_end" backend/app/services/neo4j_client.py backend/app/services/retrieval/graph_channel.py backend/app/routers/evidence.py backend/app/routers/query.py backend/app/routers/chat.py backend/app/routers/search.py backend/app/models/evidence.py backend/app/services/authority/scorer.py 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "start_char_raw" backend/app/` returns zero matches
    - `grep -rn "end_char_raw" backend/app/` returns zero matches
    - `grep -rn "span_start" backend/app/models/evidence.py` returns zero matches
    - `grep -rn "span_start" backend/app/routers/query.py` returns zero matches (CitationInfo cleaned)
    - `grep -rn "start_char_raw\|end_char_raw\|span_start\|span_end" backend/app/routers/search.py` returns zero matches
    - `grep -rn "start_char_raw\|end_char_raw\|span_start\|span_end" backend/app/services/authority/scorer.py` returns zero matches
    - `cd backend && python -m pytest tests/unit/test_cypher_queries.py -x -q` passes all tests
    - `grep "speakingRole\|speaking_role" backend/app/services/neo4j_client.py` returns at least 1 match
  </acceptance_criteria>
  <done>All 8 files cleaned of dead Cypher properties. No start_char_raw, end_char_raw, span_start, or span_end references remain in backend/app/. New schema properties (speakingRole, phaseType) exposed in vector_search return. Dead-property tests pass green.</done>
</task>

</tasks>

<verification>
- `grep -rn "start_char_raw\|end_char_raw" backend/app/` returns zero results
- `grep -rn "span_start\|span_end" backend/app/models/ backend/app/routers/query.py` returns zero results
- `grep -rn "start_char_raw\|end_char_raw\|span_start\|span_end" backend/app/routers/search.py backend/app/services/authority/scorer.py` returns zero results
- `cd backend && python -m pytest tests/ -x -q` passes
- `backend/docs/SSE_CONTRACT.md` exists and contains event tables for both chat.py and query.py
</verification>

<success_criteria>
1. Zero dead Cypher properties in backend codebase (all 8 files including scorer.py and search.py)
2. SSE contract fully documented before any router refactoring
3. Test infrastructure bootstrapped with fixtures and initial tests
4. Pydantic models cleaned (no span_start/span_end)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend/02-01-SUMMARY.md`
</output>
