---
phase: 02-backend
plan: 06
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - backend/app/routers/chat.py
  - backend/app/routers/query.py
  - backend/app/routers/survey.py
  - backend/app/routers/history.py
  - backend/app/routers/config.py
  - backend/app/routers/compass.py
  - backend/app/services/evaluation_service.py
  - backend/app/services/experts.py
  - backend/tests/unit/test_sse_contract.py
  - backend/tests/unit/test_response_shapes.py
  - backend/tests/unit/test_routers.py
autonomous: true
requirements:
  - SVC-06
  - API-01
  - API-05
  - QA-01
  - QA-02
  - QA-03

must_haves:
  truths:
    - "All service modules have English docstrings and type hints"
    - "SSE events emit in documented order with correct payload shapes"
    - "API response shapes match frontend contract"
    - "Comprehensive test suite exists with 80+ tests"
    - "Router functions are thin (input validation + service call + response)"
  artifacts:
    - path: "backend/tests/unit/test_sse_contract.py"
      provides: "Tests verifying SSE event order and payload shapes"
      contains: "test_sse"
    - path: "backend/tests/unit/test_response_shapes.py"
      provides: "Tests verifying API response shapes match frontend contract"
      contains: "test_"
    - path: "backend/tests/unit/test_routers.py"
      provides: "Tests for thin router behavior"
      contains: "test_"
  key_links:
    - from: "backend/tests/unit/test_sse_contract.py"
      to: "backend/docs/SSE_CONTRACT.md"
      via: "Tests encode the frozen contract"
      pattern: "experts.*chunk.*complete"
---

<objective>
Complete the code quality sweep: add type hints and English docstrings to all service modules, verify SSE contract compliance, ensure API response shapes are preserved, finalize router thinning, and build comprehensive test suite targeting 80+ tests.

Purpose: This is the final plan that brings the codebase to "expert-level" quality per user requirement. SVC-06 (clean naming, type hints, English docstrings), API-01 (thin routers), API-05 (preserved response shapes), and QA-01/02/03 (comprehensive tests, best practices, naming conventions) are all completed here.

Output: Polished service modules, comprehensive test suite, SSE contract tests, response shape tests.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend/02-CONTEXT.md
@.planning/phases/02-backend/02-RESEARCH.md
@.planning/phases/02-backend/02-03-SUMMARY.md
@.planning/phases/02-backend/02-04-SUMMARY.md
@.planning/phases/02-backend/02-05-SUMMARY.md

<interfaces>
<!-- SSE contract from docs/SSE_CONTRACT.md (created in Plan 01) -->
chat.py events: waiting, progress(x7), commissioni, experts(x2), citations,
  balance, compass, topic_stats, chunk, citation_details, hq_variants, complete, error
query.py events: waiting, progress(x5), experts(x2), compass, topic_stats,
  citations, citation_details, chunk, complete, error

Expert dict frozen fields:
id, first_name, last_name, group, coalition, authority_score,
relevant_speeches_count, camera_profile_url, photo, profession,
education, committee, institutional_role, score_breakdown

<!-- Frontend contract (frozen response shapes) -->
SSE payload keys: snake_case (authority_score, first_name, etc.)
chunk event: content key in chat.py, data key in query.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSE contract tests and response shape tests</name>
  <files>
    backend/tests/unit/test_sse_contract.py,
    backend/tests/unit/test_response_shapes.py
  </files>
  <read_first>
    backend/docs/SSE_CONTRACT.md,
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/routers/evaluation.py,
    backend/app/routers/evidence.py,
    backend/app/routers/search.py,
    backend/app/models/evidence.py
  </read_first>
  <action>
**test_sse_contract.py** — Tests that verify SSE events are emitted correctly:

1. `test_chat_event_types_exist`: Read chat.py source, extract all `json.dumps({'type': '...'` patterns, verify every event type from SSE_CONTRACT.md is present: `waiting`, `progress`, `commissioni`, `experts`, `citations`, `balance`, `compass`, `topic_stats`, `chunk`, `citation_details`, `hq_variants`, `complete`, `error`

2. `test_query_event_types_exist`: Same for query.py: `waiting`, `progress`, `experts`, `compass`, `topic_stats`, `citations`, `citation_details`, `chunk`, `complete`, `error`

3. `test_expert_dict_has_frozen_fields`: Import the expert construction code path (from services/experts.py), create a mock expert with all required fields, assert all frozen field names are present: `id`, `first_name`, `last_name`, `group`, `coalition`, `authority_score`, `relevant_speeches_count`, `score_breakdown`

4. `test_chat_chunk_uses_content_key`: Read chat.py source, find chunk event emission, assert it uses `content` key (not `data`)

5. `test_query_chunk_uses_data_key`: Read query.py source, find chunk event emission, assert it uses `data` key (not `content`)

6. `test_dual_experts_emission`: Read chat.py source, count occurrences of experts event emission, assert at least 2 (pre-generation and post-citation)

7. `test_sse_payloads_use_snake_case`: Read chat.py source, extract expert-related payload fields, assert `authority_score` (not `authorityScore`), `first_name` (not `firstName`)

**test_response_shapes.py** — Tests that verify API response shapes match frontend expectations:

1. `test_unified_evidence_fields`: Import UnifiedEvidence, assert it has fields: `chunk_id`, `chunk_text`, `speech_id`, `text`, `speaker_id`, `speaker_first_name`, `speaker_last_name`, `speaker_type`, `score` — and does NOT have `span_start`, `span_end`

2. `test_evidence_response_endpoint`: Use TestClient with mocked Neo4j, call GET /api/evidence/{id}, verify response contains expected keys

3. `test_search_response_shape`: Use TestClient, call POST /api/search, verify response shape

4. `test_evaluation_dashboard_response`: Use TestClient, call GET /api/evaluation/dashboard, verify response shape has expected top-level keys

5. Tests should use `app.dependency_overrides` to inject mock Neo4j that returns canned data
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/test_sse_contract.py tests/unit/test_response_shapes.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/unit/test_sse_contract.py` has at least 7 test functions
    - `backend/tests/unit/test_response_shapes.py` has at least 4 test functions
    - All tests pass
    - `grep "authority_score" backend/tests/unit/test_sse_contract.py` returns at least 1 match (frozen field verified)
  </acceptance_criteria>
  <done>SSE contract compliance verified via 7+ tests. Response shapes verified via 4+ tests. Frozen fields, event types, and payload keys all covered.</done>
</task>

<task type="auto">
  <name>Task 2: Code quality sweep — type hints, docstrings, router thinning, and remaining tests</name>
  <files>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/routers/survey.py,
    backend/app/routers/history.py,
    backend/app/routers/config.py,
    backend/app/routers/compass.py,
    backend/app/services/evaluation_service.py,
    backend/app/services/experts.py,
    backend/app/services/neo4j_client.py,
    backend/app/services/retrieval/graph_channel.py,
    backend/app/services/retrieval/engine.py,
    backend/app/services/authority/scorer.py,
    backend/tests/unit/test_routers.py
  </files>
  <read_first>
    backend/app/routers/chat.py,
    backend/app/routers/query.py,
    backend/app/routers/survey.py,
    backend/app/routers/history.py,
    backend/app/services/experts.py,
    backend/app/services/evaluation_service.py,
    backend/app/services/neo4j_client.py
  </read_first>
  <action>
**Code quality sweep (SVC-06, QA-02, QA-03):**

For EACH service module (`neo4j_client.py`, `graph_channel.py`, `engine.py`, `scorer.py`, `experts.py`, `evaluation_service.py`):
1. Add module-level English docstring if missing
2. Add English docstrings to all public functions (not private helpers unless complex)
3. Add type hints to all function signatures that lack them
4. Remove any Italian comments — replace with English equivalents
5. Remove any dead code (commented-out blocks, unreachable branches)
6. Ensure consistent naming: snake_case for Python, camelCase for Neo4j properties in Cypher strings

**Router thinning (API-01):**

For `chat.py` and `query.py` — verify that after Plans 03 and 04, the routers are thin:
- No expert computation in routers (delegated to services/experts.py)
- No metric computation in routers (delegated to services/evaluation_service.py)
- SSE streaming generators may remain in routers (they are HTTP-layer concerns), but the pipeline logic they call should be in services
- If any remaining business logic functions are found in chat.py or query.py that aren't HTTP-specific, extract them to appropriate services

For other routers (`survey.py`, `history.py`, `config.py`, `compass.py`):
- Check for business logic that should be in services
- Add type hints and English docstrings
- Survey.py: verify `_load_surveys` and `_calculate_stats` are now imported from services (done in Plan 03)

**test_routers.py:**
Create `backend/tests/unit/test_routers.py`:
- `test_chat_router_no_business_logic`: Read chat.py source, assert no `def _compute_` private functions remain that do business logic (expert computation, metric calculation). Helper functions for HTTP formatting are acceptable.
- `test_query_router_no_business_logic`: Same for query.py
- `test_all_routers_have_docstrings`: For each router file, check that the module has a docstring
- `test_all_services_have_docstrings`: For each service file, check module docstring exists
- `test_no_italian_comments`: Read all Python files in `app/`, search for common Italian words in comments (`# calcola`, `# carica`, `# salva`, `# controlla`, `# gestisce`). Allow Italian in string literals (user-facing messages stay in Italian).

**Final test count verification:**
After all tests are written, run the full suite and count. Target: 80+ tests across all test_*.py files. If below 80, add tests for:
- Router endpoint status codes (200 for valid, 404 for missing, etc.)
- Edge cases in expert computation (empty evidence, single party, etc.)
- Edge cases in evaluation service (no chats, single chat, etc.)
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/ -q` shows 80+ tests passing
    - `grep -rn "def _compute_experts" backend/app/routers/` returns zero matches
    - Every .py file in `backend/app/services/` has a module docstring (first non-empty line contains `"""`)
    - `grep -rn "# calcola\|# carica\|# salva" backend/app/` returns zero matches (Italian comments removed)
    - `cd backend && python -m pytest tests/unit/test_routers.py -x -q` passes
  </acceptance_criteria>
  <done>All service modules have English docstrings and type hints. All routers are thin (no business logic). 80+ tests pass. No Italian comments in code. QA-01, QA-02, QA-03 satisfied.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -q` shows 80+ tests, all passing
- `cd backend && python -m pytest tests/ -q --tb=no | grep "passed"` confirms count
- No `_compute_experts` definitions in any router file
- All service modules have English docstrings
- SSE contract tests verify frozen event shapes
- Response shape tests verify frontend contract
</verification>

<success_criteria>
1. 80+ tests passing in comprehensive suite
2. All service modules have English docstrings and type hints
3. All routers are thin wrappers
4. SSE contract verified via automated tests
5. API response shapes verified via automated tests
6. No Italian comments in code
7. Consistent naming (snake_case Python, camelCase Neo4j)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend/02-06-SUMMARY.md`
</output>
