---
phase: 05-multi-language-support
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/messages/it.json
  - frontend/messages/en.json
  - frontend/src/config/index.ts
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/CitationCard.tsx
  - frontend/src/components/chat/ExpertCard.tsx
  - frontend/src/components/chat/CompassCard.tsx
  - frontend/src/components/chat/TopicStatsModal.tsx
  - frontend/src/components/shared/ProgressIndicator.tsx
  - frontend/src/components/shared/HistoryModal.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/hooks/use-chat.ts
  - frontend/src/app/layout.tsx
autonomous: true
requirements: [ML-02]

must_haves:
  truths:
    - "All hardcoded Italian UI text is replaced with translation keys via useTranslations()"
    - "Italian locale file contains all extracted strings in Italian"
    - "English locale file contains English translations for all keys"
    - "When locale is Italian, UI text is identical to current production"
    - "config/index.ts progressSteps uses IDs only, labels moved to locale files"
  artifacts:
    - path: "frontend/messages/it.json"
      provides: "Complete Italian locale with all UI strings"
      min_lines: 100
    - path: "frontend/messages/en.json"
      provides: "Complete English locale with all UI strings"
      min_lines: 100
    - path: "frontend/src/config/index.ts"
      provides: "Config with progress step IDs, no Italian label strings"
  key_links:
    - from: "frontend/src/components/chat/MessageBubble.tsx"
      to: "frontend/messages/*.json"
      via: "useTranslations('MessageBubble')"
      pattern: "useTranslations"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/messages/*.json"
      via: "useTranslations('Sidebar')"
      pattern: "useTranslations"
    - from: "frontend/src/components/shared/ProgressIndicator.tsx"
      to: "frontend/messages/*.json"
      via: "useTranslations('ProgressIndicator')"
      pattern: "useTranslations"
---

<objective>
Extract all ~150 hardcoded Italian UI strings across 13+ files to translation keys, populate both it.json and en.json locale files, and refactor config/index.ts to separate data (IDs, icons) from translatable labels.

Purpose: This is the bulk i18n migration work -- every user-visible string moves from inline Italian to a translation key, enabling the language selector (from Plan 01) to actually switch the UI language.
Output: Complete it.json and en.json locale files; all components using useTranslations() instead of hardcoded strings.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-language-support-with-translated-citations-and-original-text-hover-disclaimer/05-CONTEXT.md
@.planning/phases/05-multi-language-support-with-translated-citations-and-original-text-hover-disclaimer/05-RESEARCH.md
@.planning/phases/05-multi-language-support-with-translated-citations-and-original-text-hover-disclaimer/05-01-SUMMARY.md

<interfaces>
<!-- next-intl client usage pattern (from Plan 01) -->
```typescript
// In any "use client" component:
import { useTranslations } from 'next-intl';
const t = useTranslations('NamespaceName');
// t('keyName') returns the translated string
// t('nested.key') for dot-path access within namespace
```

<!-- Locale file structure (from Plan 01) -->
```json
// messages/it.json and messages/en.json have identical key structure
{
  "NamespaceName": {
    "keyName": "translated value"
  }
}
```

<!-- Config progress steps that need refactoring -->
From frontend/src/config/index.ts:
```typescript
progressSteps: [
  { id: 1, label: "Analisi query", description: "...", icon: "Search", whyDescription: "..." },
  // ... 8 steps total, each with label, description, whyDescription in Italian
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract high-priority component strings to locale files</name>
  <files>
    frontend/messages/it.json,
    frontend/messages/en.json,
    frontend/src/config/index.ts,
    frontend/src/components/chat/MessageBubble.tsx,
    frontend/src/components/shared/ProgressIndicator.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/hooks/use-chat.ts,
    frontend/src/app/layout.tsx
  </files>
  <read_first>
    frontend/src/config/index.ts,
    frontend/src/components/chat/MessageBubble.tsx,
    frontend/src/components/shared/ProgressIndicator.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/hooks/use-chat.ts,
    frontend/src/app/layout.tsx,
    frontend/messages/it.json,
    frontend/messages/en.json
  </read_first>
  <action>
This task handles the ~6 highest-string-count files (~90 strings). For EACH file:
1. Read the file and identify every Italian string visible to users
2. Create a namespace in both it.json and en.json
3. Replace the hardcoded string with `t('key')` using `useTranslations('Namespace')`

**config/index.ts** (special handling per Pitfall 4 in RESEARCH.md):
- Remove `label`, `description`, and `whyDescription` from progressSteps entries
- Keep only `id` and `icon` in the array
- Remove the `as const` if present on progressSteps since the type changes
- Create `ProgressSteps` namespace in locale files with keys like `step1.label`, `step1.description`, `step1.whyDescription` for all 8 steps
- Also extract `chat.placeholder`, `chat.welcomeMessage`, and `app.description` to locale files
- Keep non-translatable config values (URLs, numbers, dimensions) in config

**MessageBubble.tsx** (~25 strings):
- Namespace: `MessageBubble`
- Extract: "Tu", "Camera dei Deputati -- XIX Legislatura", "Fonti selezionate per autorita", "Citazioni", "Bilanciamento", "Bussola Ideologica", "Analisi High Quality (Best-of-N)", "Motivazione della scelta", "Variante A/B", "Vincitore", "Link copiato negli appunti", "Condividi", "Maggioranza", "Opposizione", "Compasso ideologico", all section tooltip texts
- Add `import { useTranslations } from 'next-intl';` and `const t = useTranslations('MessageBubble');`

**ProgressIndicator.tsx** (~20 strings):
- Namespace: `ProgressIndicator`
- Extract: "In corso...", "Tu", "Connessione...", all step labels/descriptions (now read from locale, not config), waiting message templates
- The component currently reads step labels from config.ui.progressSteps -- change it to use `t('steps.step{id}.label')` instead

**Sidebar.tsx** (~12 strings):
- Namespace: `Sidebar`
- Extract: "Ricerca Topic", "Strumenti", "Ricerca Atti", "Analisi Autorita", "Compasso Ideologico", "Dati aggiornati al", "Impostazioni", "Documentazione", "Espandi menu", "Universita degli Studi di Milano Bicocca"

**use-chat.ts** (~10 strings):
- Namespace: `Chat` (shared with hook)
- Extract: "Connessione...", "In attesa...", "Attualmente troppi utenti...", Italian progress labels, error messages
- NOTE: use-chat.ts is a "use client" hook so `useTranslations` works directly. Add a `t` parameter or import useTranslations inside the hook and call it at the top level of the hook function (not inside callbacks -- hooks rules). For strings used inside callbacks, capture `t` reference at hook level.

**layout.tsx** (~8 strings):
- The metadata.description and keywords are server-side SEO strings. These can stay hardcoded in Italian for SEO purposes OR be made dynamic. Since the metadata export is static (not inside a function that has access to locale), keep metadata in Italian for now (Italian parliament SEO).
- Extract MaintenancePage strings to `Maintenance` namespace
- The `lang` attribute is already dynamic from Plan 01

After all extractions, verify the locale files have matching key structures:
```bash
cd frontend && node -e "const it=Object.keys(JSON.parse(require('fs').readFileSync('messages/it.json','utf8'))); const en=Object.keys(JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))); console.log('IT namespaces:', it.length, 'EN namespaces:', en.length); console.log('Match:', JSON.stringify(it)===JSON.stringify(en));"
```

Then verify tsc:
```bash
cd frontend && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useTranslations" frontend/src/components/chat/MessageBubble.tsx` matches
    - `grep "useTranslations" frontend/src/components/shared/ProgressIndicator.tsx` matches
    - `grep "useTranslations" frontend/src/components/layout/Sidebar.tsx` matches
    - `grep -c "label" frontend/src/config/index.ts` shows reduced count (labels moved to locale)
    - `node -e "const it=require('./frontend/messages/it.json'); const en=require('./frontend/messages/en.json'); console.log(JSON.stringify(Object.keys(it).sort())===JSON.stringify(Object.keys(en).sort()))"` prints true
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>High-priority components (~90 strings) extracted to locale files; config refactored to data-only; tsc passes</done>
</task>

<task type="auto">
  <name>Task 2: Extract remaining component strings and verify completeness</name>
  <files>
    frontend/messages/it.json,
    frontend/messages/en.json,
    frontend/src/components/chat/CitationCard.tsx,
    frontend/src/components/chat/ExpertCard.tsx,
    frontend/src/components/chat/CompassCard.tsx,
    frontend/src/components/chat/TopicStatsModal.tsx,
    frontend/src/components/shared/HistoryModal.tsx
  </files>
  <read_first>
    frontend/src/components/chat/CitationCard.tsx,
    frontend/src/components/chat/ExpertCard.tsx,
    frontend/src/components/chat/CompassCard.tsx,
    frontend/src/components/chat/TopicStatsModal.tsx,
    frontend/src/components/shared/HistoryModal.tsx,
    frontend/messages/it.json,
    frontend/messages/en.json
  </read_first>
  <action>
Extract remaining Italian strings from medium-priority files (~25 strings total):

**CitationCard.tsx** (~6 strings):
- Update the `CitationCard` namespace (skeleton from Plan 01) with any remaining strings
- Extract: "Vai all'intervento originale", "Contesto Parlamentare", "Intervento", "Vai all'intervento sul sito della Camera", any date labels, coalition labels like "Governo"

**ExpertCard.tsx** (~5 strings):
- Namespace: `ExpertCard`
- Extract expert display labels (profession, education, committee headers, authority score label)

**CompassCard.tsx** (~5 strings):
- Namespace: `CompassCard`
- Extract: "Significato:", axis labels, any section headers

**TopicStatsModal.tsx** (~5 strings):
- Namespace: `TopicStatsModal`
- Extract: "Dettaglio Statistiche", table headers, metric labels

**HistoryModal.tsx** (~5 strings):
- Namespace: `HistoryModal`
- Extract: "Cronologia Chat", search placeholder, empty state text

For each file:
1. Add `import { useTranslations } from 'next-intl';`
2. Add `const t = useTranslations('Namespace');`
3. Replace each Italian string with `t('key')`
4. Add keys to BOTH it.json and en.json

After all extractions, run a final grep check for remaining Italian strings:
```bash
grep -rn '"[A-Z][a-z]*[àèéìòù]' frontend/src/components/ frontend/src/hooks/ frontend/src/config/ --include='*.tsx' --include='*.ts' | grep -v 'node_modules' | grep -v '.json' | head -20
```

Verify build:
```bash
cd frontend && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useTranslations" frontend/src/components/chat/CitationCard.tsx` matches
    - `grep "useTranslations" frontend/src/components/chat/ExpertCard.tsx` matches
    - `grep "useTranslations" frontend/src/components/chat/CompassCard.tsx` matches
    - `grep "useTranslations" frontend/src/components/chat/TopicStatsModal.tsx` matches
    - `grep "useTranslations" frontend/src/components/shared/HistoryModal.tsx` matches
    - it.json and en.json have identical namespace key structures
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All ~150 Italian UI strings extracted to translation keys; both locale files complete; zero hardcoded Italian in user-facing components; tsc passes</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` exits 0
2. `node -e "const it=require('./frontend/messages/it.json'); const en=require('./frontend/messages/en.json'); const ik=JSON.stringify(Object.keys(it).sort()); const ek=JSON.stringify(Object.keys(en).sort()); console.log('Match:', ik===ek, 'Namespaces:', Object.keys(it).length);"` shows Match: true with 10+ namespaces
3. No accented Italian strings remain in component files (verified by grep)
</verification>

<success_criteria>
- All ~150 hardcoded Italian UI strings extracted to translation keys
- it.json contains all strings in Italian (identical to current production text)
- en.json contains English translations for all keys
- config/index.ts progressSteps has only id+icon, no Italian text
- All modified components use useTranslations() from next-intl
- tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-language-support-with-translated-citations-and-original-text-hover-disclaimer/05-02-SUMMARY.md`
</output>
