---
phase: 14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page
plan: 06
type: execute
wave: 4
depends_on: ["14-04", "14-05"]
files_modified:
  - frontend/src/types/chat.ts
  - frontend/src/hooks/use-chat.ts
  - frontend/src/components/chat/MessageBubble.tsx
autonomous: true
requirements: [VI-01, VI-04]
must_haves:
  truths:
    - "The chat hook captures the vote_coherence SSE event and stores it on the assistant message"
    - "MessageBubble renders a 'come hanno votato' editorial block for citation groups whose session has votes"
    - "The block shows overall outcome (Esito) plus per-party favor/against/abstain chips when available"
    - "When party breakdown is empty, only the aggregate outcome row renders (aggregate degradation)"
    - "The chat hook captures the vote_facts SSE event and stores it on the assistant message"
    - "MessageBubble renders F4 vote-fact chips linking to the debate, visually distinct from citations and exempt from citation-verification"
  artifacts:
    - path: "frontend/src/types/chat.ts"
      provides: "voteCoherence + voteFacts fields on Message + VoteCoherence/VoteFactChip types"
      contains: "voteCoherence"
    - path: "frontend/src/hooks/use-chat.ts"
      provides: "vote_coherence + vote_facts SSE cases"
      contains: "vote_coherence"
    - path: "frontend/src/components/chat/MessageBubble.tsx"
      provides: "vote coherence editorial block + vote-fact chip rendering"
      contains: "voteCoherence"
  key_links:
    - from: "frontend/src/hooks/use-chat.ts"
      to: "frontend/src/components/chat/MessageBubble.tsx"
      via: "message.voteCoherence"
      pattern: "voteCoherence"
    - from: "frontend/src/hooks/use-chat.ts"
      to: "frontend/src/components/chat/MessageBubble.tsx"
      via: "message.voteFacts"
      pattern: "voteFacts"
---

<objective>
Render F1 speech-vote coherence AND F4 vote-fact chips in chat. Capture the `vote_coherence` and `vote_facts` SSE events in `use-chat.ts`, store them on the assistant `Message`, render a compact editorial "come hanno votato" block under citation groups in `MessageBubble.tsx` (F1), and render clickable vote-fact chips that link to the debate (F4).

Purpose: Surfaces how cited parties actually voted (F1, the flagship feature) and the vote-fact chip references the F4 locked decision requires — the chip the 14-08 human gate explicitly checks for.
Output: chat type fields + two SSE captures + MessageBubble editorial block + vote-fact chips.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md

<interfaces>
SSE events (emitted by query.py in 14-04):
  vote_coherence (14-04 Task 2):
    { "type": "vote_coherence", "data": {
        "<session_id>": { "votes": [{vote_id, label, outcome, in_favor, against,
           party_breakdown: [{party, favor, against, abstain}]}], "debate_id": "..." } } }
  vote_facts (14-04 Task 3):
    { "type": "vote_facts", "data": [ { vote_id: string, debate_id: string | null, label: string } ] }
    Chips are graph lookups — exempt from the citation-verifier.

use-chat.ts (frontend/src/hooks/use-chat.ts) existing SSE handling:
  - switch/case on data.type inside the stream loop (~L286 case "task_id", ~L384 case "experts")
  - local accumulators declared ~L229-231 (experts, compassData)
  - updateLastAssistantMessage({...}) merges into the assistant Message
  - on "complete" (~L250) the final message object is assembled with experts, compass, etc.

Message type (frontend/src/types/chat.ts): has citations?, experts?, compass? — ADD voteCoherence? and voteFacts?
Citations carry session_id (Citation type — verify field name; backend citation_details includes session_id).

MessageBubble.tsx: renders sections and citation links; groups citations by section.
i18n keys already added in 14-05:
  namespace "VoteCoherence": heading, outcomePrefix, favor, against, abstain, onlyAggregate — use useTranslations('VoteCoherence').
  namespace "VoteFacts": chipAria, chipPrefix — use useTranslations('VoteFacts').
Vote types (VoteExplorerEntry etc.) re-exported from @/types barrel by 14-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voteCoherence to Message type + capture the SSE event in use-chat</name>
  <read_first>
    - frontend/src/types/chat.ts (Message interface ~L123, HQVariant, Citation shape and its session_id field)
    - frontend/src/hooks/use-chat.ts (SSE switch ~L286-390, accumulators ~L229-231, "complete" assembly ~L250-256, updateLastAssistantMessage helper)
    - frontend/src/types/votes.ts (optionally reuse VoteExplorerEntry-adjacent shapes; else define VoteCoherenceData here)
  </read_first>
  <action>
    1. In `frontend/src/types/chat.ts`, add types:
       ```typescript
       export interface VoteCoherencePartyBreakdown { party: string; favor: number; against: number; abstain: number; }
       export interface VoteCoherenceVote { vote_id: string; label: string; outcome: string; in_favor: number; against: number; party_breakdown: VoteCoherencePartyBreakdown[]; }
       export interface VoteCoherenceSession { votes: VoteCoherenceVote[]; debate_id: string | null; }
       export type VoteCoherenceData = Record<string, VoteCoherenceSession>;
       ```
       Add `voteCoherence?: VoteCoherenceData;` to the `Message` interface (next to `compass?`).

    2. In `use-chat.ts`:
       - Declare an accumulator near experts/compassData: `let voteCoherence: VoteCoherenceData | undefined = undefined;`
       - Add a `case "vote_coherence":` in the SSE switch that sets `voteCoherence = data.data;` and calls `updateLastAssistantMessage({ voteCoherence });`.
       - In the `"complete"` message assembly, include `voteCoherence` in the final message object (same place experts/compass are added).
       - Import the new type: `import type { VoteCoherenceData } from "@/types";` (ensure it's exported from the barrel).
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && grep -q "voteCoherence" src/types/chat.ts && grep -q "vote_coherence" src/hooks/use-chat.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/chat.ts` contains `voteCoherence?` on Message and `export type VoteCoherenceData`
    - `src/hooks/use-chat.ts` contains `case "vote_coherence"` and `voteCoherence` in the complete-message assembly
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>vote_coherence SSE event is captured and persisted on the assistant Message with full typing.</done>
</task>

<task type="auto">
  <name>Task 2: Render the "come hanno votato" editorial block in MessageBubble</name>
  <read_first>
    - frontend/src/components/chat/MessageBubble.tsx (how citation groups/sections render ~L339-406; where message.citations/experts are consumed; existing editorial/hairline classNames)
    - frontend/src/types/chat.ts (VoteCoherenceData from Task 1; Citation.session_id field)
    - frontend/messages/it.json (VoteCoherence namespace keys added in 14-05)
    - frontend/src/app/compass/page.tsx (Fraunces serif-numeral className reference for consistency)
  </read_first>
  <action>
    In `MessageBubble.tsx`, add a helper sub-component `VoteCoherenceBlock` and render it under the citation grouping when `message.voteCoherence` is present:

    - `const tvc = useTranslations('VoteCoherence');`
    - Determine which session_ids are referenced by the message's citations (map `message.citations` → their `session_id`), then for each matching key in `message.voteCoherence`, render one block per vote (limit already applied server-side to <=5).
    - Block style: a hairline rule above (`border-t` thin), heading `tvc('heading')` ("Come hanno votato"), then per vote:
      - Aggregate row: `{tvc('outcomePrefix')}: {outcome} {in_favor}-{against}` using the Fraunces serif-numeral className for the numbers.
      - If `party_breakdown` has entries with any nonzero favor/against/abstain: render one chip per party showing `{party}` + colored favor/against/abstain counts (green favor, red against, neutral abstain). If ALL breakdown entries are zero or the array is empty, render `tvc('onlyAggregate')` note instead (aggregate degradation — Pitfall 3).
    - Do NOT render the block if no citation session_id matches a voteCoherence key.
    - Keep it compact and editorial (no cards; chips are inline pills). Match the app's existing chip/badge styling (grep for existing badge classes).
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && grep -q "VoteCoherence" src/components/chat/MessageBubble.tsx && npx tsc --noEmit -p tsconfig.json && npx eslint src/components/chat/MessageBubble.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MessageBubble.tsx` contains `message.voteCoherence` and `useTranslations('VoteCoherence')`
    - `MessageBubble.tsx` contains a branch rendering `tvc('onlyAggregate')` for empty/zero party_breakdown
    - `MessageBubble.tsx` references `party_breakdown`
    - `npx tsc --noEmit` exits 0 and eslint on MessageBubble.tsx reports no errors
  </acceptance_criteria>
  <done>Under citation groups, the vote block shows overall outcome plus per-party chips, degrading to aggregate-only when individual data is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Capture vote_facts SSE + render F4 vote-fact chips in MessageBubble</name>
  <read_first>
    - frontend/src/hooks/use-chat.ts (SSE switch + accumulators + "complete" assembly; the `case "vote_coherence"` added in Task 1 — mirror it)
    - frontend/src/types/chat.ts (Message interface; voteCoherence field added in Task 1)
    - frontend/src/components/chat/MessageBubble.tsx (how existing citation chips/links are styled and how they navigate; the VoteCoherenceBlock added in Task 2)
    - frontend/messages/it.json (VoteFacts namespace added in 14-05: chipAria, chipPrefix)
  </read_first>
  <action>
    F4 emits a `vote_facts` SSE event (added in 14-04 Task 3) with payload:
      `{ "type": "vote_facts", "data": [{ vote_id: string; debate_id: string | null; label: string }] }`
    These are clickable chips that link to the voted debate; they are NOT citations and MUST NOT pass through the citation-verifier.

    1. In `frontend/src/types/chat.ts`, add:
       ```typescript
       export interface VoteFactChip { vote_id: string; debate_id: string | null; label: string; }
       ```
       Add `voteFacts?: VoteFactChip[];` to the `Message` interface (next to `voteCoherence?`).

    2. In `frontend/src/hooks/use-chat.ts`:
       - Declare an accumulator near voteCoherence: `let voteFacts: VoteFactChip[] | undefined = undefined;`
       - Add `case "vote_facts":` in the SSE switch that sets `voteFacts = data.data;` and calls `updateLastAssistantMessage({ voteFacts });`.
       - Include `voteFacts` in the `"complete"` message assembly (same place voteCoherence is added).
       - Import: `import type { VoteFactChip } from "@/types";` (VoteFactChip lives in chat.ts; ensure chat.ts is re-exported from the barrel — it already is for VoteCoherenceData used in Task 1).

    3. In `frontend/src/components/chat/MessageBubble.tsx`, render a vote-fact chip row when `message.voteFacts?.length`:
       - `const tvf = useTranslations('VoteFacts');`
       - For each fact, render an inline pill/chip (match the existing citation-chip styling — grep the existing citation chip className) showing `fact.label` (optionally prefixed with `tvf('chipPrefix')`), with `aria-label={tvf('chipAria')}`.
       - The chip is an anchor/button navigating to `/transcript/${fact.debate_id}` when `debate_id` is present, else `/timeline` (use the same navigation approach existing citation links use — e.g. window.location.href or the router the file already imports).
       - Render this row visually distinct from standard citation chips (these are vote references, exempt from citation-verification). Do NOT feed `voteFacts` into any citation-verification / hallucination-check path.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && grep -q "voteFacts" src/types/chat.ts && grep -q "vote_facts" src/hooks/use-chat.ts && grep -q "voteFacts" src/components/chat/MessageBubble.tsx && npx tsc --noEmit -p tsconfig.json && npx eslint src/components/chat/MessageBubble.tsx src/hooks/use-chat.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/chat.ts` contains `export interface VoteFactChip` and `voteFacts?` on Message
    - `src/hooks/use-chat.ts` contains `case "vote_facts"` and `voteFacts` in the complete-message assembly
    - `MessageBubble.tsx` contains `message.voteFacts` and `useTranslations('VoteFacts')` and navigates using `debate_id`
    - `npx tsc --noEmit` exits 0 and eslint on the two listed files reports no errors
  </acceptance_criteria>
  <done>vote_facts SSE event captured and rendered as clickable vote chips linking to the debate, visually distinct from citations and exempt from citation-verification — satisfying the F4 chip check in the 14-08 human gate.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` green; `npx eslint src/components/chat/MessageBubble.tsx src/hooks/use-chat.ts` clean
- vote_coherence event captured end-to-end and rendered as an editorial block scoped to cited sessions
- vote_facts event captured end-to-end and rendered as clickable chips linking to the debate
</verification>

<success_criteria>
F1 block renders beneath citations for sessions with votes (per-party breakdown when available, aggregate-only otherwise). F4 vote-fact chips render and link to the debate, exempt from citation-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-06-SUMMARY.md`
</output>
