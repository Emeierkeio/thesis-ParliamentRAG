---
phase: 14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/compass/vote_pipeline.py
  - backend/app/routers/compass.py
  - backend/tests/unit/test_vote_compass.py
autonomous: true
requirements: [VI-02]
must_haves:
  truths:
    - "build_party_vote_matrix encodes favor=1.0, against=0.0, abstain=0.5, missing=0.5"
    - "SVD-PCA projects parties to 2D and returns variance_explained summing to <= 1.0"
    - "GET /api/compass/votes?legislature=&chamber= returns party coordinates + variance explained"
    - "Vote compass results are cached per (legislature, chamber) with a TTL"
    - "Endpoint returns {available: false, reason: individual_votes_pending} when < 3 parties have vote data"
  artifacts:
    - path: "backend/app/services/compass/vote_pipeline.py"
      provides: "party×vote matrix, numpy SVD PCA, TTL cache, endpoint logic"
      contains: "def build_party_vote_matrix"
    - path: "backend/app/routers/compass.py"
      provides: "GET /api/compass/votes endpoint"
      contains: "/compass/votes"
    - path: "backend/tests/unit/test_vote_compass.py"
      provides: "Unit tests for matrix encoding, PCA shape, cache TTL"
      contains: "def test_party_vote_matrix"
  key_links:
    - from: "backend/app/routers/compass.py"
      to: "backend/app/services/compass/vote_pipeline.py"
      via: "get_vote_compass(legislature, chamber, neo4j)"
      pattern: "vote_pipeline|get_vote_compass"
---

<objective>
Build the vote-based ideological compass (F2): a party×vote matrix reduced to 2 dimensions via numpy SVD-PCA, exposed at a NEW endpoint `GET /api/compass/votes`. This is separate from the existing query-dependent text compass (`POST /api/compass`) — do NOT modify the text compass (Pitfall 6).

Purpose: Separates "what parties say" (text compass) from "what they do" (vote compass). Query-independent, cached, computed on demand.
Output: `vote_pipeline.py` (math + cache + endpoint logic), a new route in `compass.py`, and unit tests.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md

<interfaces>
Existing compass router (backend/app/routers/compass.py):
  router = APIRouter(prefix="/api", tags=["Compass"])
  @router.post("/compass")  async def compass_endpoint(request: CompassRequest)
  from .query import get_services   # get_services()["neo4j"] gives the Neo4jClient

numpy is available (requirements.txt numpy>=1.24.0). scipy is available but AVOID importing it
(anaconda env has NumPy 2.x/scipy incompatibility — tests must import only numpy + vote_pipeline).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vote_pipeline.py — party×vote matrix, SVD PCA, TTL cache</name>
  <read_first>
    - backend/app/services/compass/pipeline.py (how compass services are structured; Neo4jClient usage)
    - backend/app/services/neo4j_client.py (exact query method signature)
    - .planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md (F2 section — matrix construction, SVD, cache code verbatim)
  </read_first>
  <behavior>
    - build_party_vote_matrix on rows [{party:'A',vote_id:'v1',favor:10,against:0,abstain:0},{party:'B',vote_id:'v1',favor:0,against:8,abstain:0}] returns matrix with A=1.0, B=0.0 on v1
    - a party/vote combo with favor==against encodes 0.5; a missing combo defaults to 0.5
    - SVD projection of a (n_parties x n_votes) matrix returns coords shape (n_parties, 2)
    - variance_explained is a list whose sum is <= 1.0 + 1e-9
    - cache returns the identical object within TTL and recomputes after TTL expiry
  </behavior>
  <action>
    Create `backend/app/services/compass/vote_pipeline.py` with:

    1. Module constant `PARTY_VOTE_MATRIX_QUERY` = the exact Cypher from 14-RESEARCH.md F2 section (MATCH Session {chamber, legislature} HAS_VOTE Vote; VOTED IndividualVote ON_VOTE; MEMBER_OF_GROUP date-scoped; RETURN vote_id, party, favor, against, abstain).

    2. `build_party_vote_matrix(rows: list[dict]) -> tuple[np.ndarray, list[str], list[str]]` — encoding: favor > against → 1.0; against > favor → 0.0; equal → 0.5; total==0 → leave default 0.5; unspecified combos default 0.5. Initialize `matrix = np.full((len(parties), len(votes)), 0.5)`. Return (matrix, parties, votes). Copy the reference implementation from 14-RESEARCH.md verbatim.

    3. `pca_2d(matrix: np.ndarray) -> tuple[np.ndarray, list[float]]` — center per vote: `M = matrix - matrix.mean(axis=0, keepdims=True)`; `U,S,Vt = np.linalg.svd(M, full_matrices=False)`; `variance_explained = ((S**2)/(S**2).sum()).tolist()`; `coords = (U[:, :2] * S[:2])`. Return (coords, variance_explained). Guard: if matrix has < 2 rows or < 2 columns, return zeros of shape (n,2) and [].

    4. TTL cache: `_vote_compass_cache: dict[tuple[int,str], tuple[dict,float]] = {}` and `VOTE_COMPASS_TTL_SECONDS = 3600`, plus `_compute_vote_compass(legislature, chamber, neo4j)` and `get_vote_compass(legislature, chamber, neo4j)` exactly as in 14-RESEARCH.md. `_compute_vote_compass` runs the query, builds matrix, runs pca_2d, and returns `{"available": True, "legislature": legislature, "chamber": chamber, "parties": [{"party":p, "x": round(float(coords[i,0]),4), "y": round(float(coords[i,1]),4)} for i,p in enumerate(parties)], "variance_explained": [round(v,4) for v in variance_explained[:2]]}`. If fewer than 3 parties have rows, return `{"available": False, "reason": "individual_votes_pending"}` (Pitfall 3).

    Create `backend/tests/unit/test_vote_compass.py` with tests: `test_party_vote_matrix` (encoding assertions above), `test_pca_shape` (coords.shape == (n,2) and sum(variance_explained) <= 1.0+1e-9), `test_compass_cache` (monkeypatch `_compute_vote_compass` to a counter; two calls within TTL invoke it once). Import only `import numpy as np` and `from app.services.compass.vote_pipeline import ...`.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/test_vote_compass.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `vote_pipeline.py` contains `def build_party_vote_matrix(`, `def pca_2d(`, `def get_vote_compass(`, `VOTE_COMPASS_TTL_SECONDS`
    - `grep -c "s.legislature" backend/app/services/compass/vote_pipeline.py` >= 1 and contains `mg.start_date <= s.date`
    - `vote_pipeline.py` contains `np.linalg.svd` and does NOT contain `import scipy`
    - `test_vote_compass.py` contains `def test_party_vote_matrix`, `def test_pca_shape`, `def test_compass_cache`
    - `python -m pytest tests/unit/test_vote_compass.py -x -q` exits 0
  </acceptance_criteria>
  <done>Matrix encoding, PCA shape, and cache TTL tests pass with numpy only.</done>
</task>

<task type="auto">
  <name>Task 2: Add GET /api/compass/votes endpoint to compass.py</name>
  <read_first>
    - backend/app/routers/compass.py (existing POST /compass endpoint, get_services import, error handling)
    - backend/app/services/compass/vote_pipeline.py (get_vote_compass signature from Task 1)
  </read_first>
  <action>
    In `backend/app/routers/compass.py`, add a NEW endpoint (do not touch the existing `POST /compass`):

    ```python
    from ..services.compass.vote_pipeline import get_vote_compass

    @router.get("/compass/votes")
    async def vote_compass_endpoint(legislature: int = 19, chamber: str = "camera"):
        """Vote-based compass: party coordinates from IndividualVote PCA. Query-independent, cached."""
        try:
            neo4j = get_services()["neo4j"]
            result = await asyncio.get_running_loop().run_in_executor(
                None, lambda: get_vote_compass(legislature, chamber, neo4j)
            )
            return result
        except Exception as e:
            logger.error(f"[COMPASS-VOTES] Failed: {e}", exc_info=True)
            raise HTTPException(status_code=500, detail=str(e))
    ```

    Note: chamber default is a single chamber ("camera"), NOT "both" — a vote compass mixes political systems if chambers are combined (Pitfall 1). The frontend passes an explicit chamber. Ensure `asyncio` and `logger` are already imported at the top of compass.py (they are — verify).
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/test_vote_compass.py -x -q && python -c "import ast,sys; src=open('app/routers/compass.py').read(); assert '/compass/votes' in src and 'get_vote_compass' in src; ast.parse(src)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/compass.py` contains `@router.get("/compass/votes")` and `get_vote_compass`
    - `grep -c "@router.post(\"/compass\")" backend/app/routers/compass.py` == 1 (existing text compass untouched)
    - `python -c "import ast; ast.parse(open('backend/app/routers/compass.py').read())"` exits 0
    - verify command exits 0
  </acceptance_criteria>
  <done>New GET /api/compass/votes endpoint wired to get_vote_compass; text compass unchanged.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_vote_compass.py -x -q` green (3 tests)
- compass.py has both POST /compass (unchanged) and GET /compass/votes (new)
- No scipy import anywhere in vote_pipeline.py
</verification>

<success_criteria>
Vote compass endpoint returns cached party coordinates + variance; degrades to available:false with < 3 parties; text compass untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-02-SUMMARY.md`
</output>
