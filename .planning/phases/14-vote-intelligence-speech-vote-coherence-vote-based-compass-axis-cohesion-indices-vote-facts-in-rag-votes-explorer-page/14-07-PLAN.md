---
phase: 14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page
plan: 07
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-05"]
files_modified:
  - frontend/src/app/compass/page.tsx
  - frontend/src/app/rankings/page.tsx
autonomous: true
requirements: [VI-02, VI-03]
must_haves:
  truths:
    - "The compass page has an 'Assi testuali | Assi di voto' segmented toggle; text compass stays the default"
    - "Selecting 'Assi di voto' loads party coordinates from /api/compass/votes and renders them on the compass"
    - "The rankings page has vote metric columns (Rice cohesion, rebellion, participation) behind a toggle"
    - "Vote columns are hidden (not zeroed) when the endpoint returns available:false"
  artifacts:
    - path: "frontend/src/app/compass/page.tsx"
      provides: "text/vote axis toggle + vote compass rendering"
      contains: "getVoteCompass"
    - path: "frontend/src/app/rankings/page.tsx"
      provides: "cohesion columns/toggle"
      contains: "getVoteCohesion"
  key_links:
    - from: "frontend/src/app/compass/page.tsx"
      to: "frontend/src/lib/votes-api.ts"
      via: "getVoteCompass(legislature, chamber)"
      pattern: "getVoteCompass"
    - from: "frontend/src/app/rankings/page.tsx"
      to: "frontend/src/lib/votes-api.ts"
      via: "getVoteCohesion(chamber, legislature)"
      pattern: "getVoteCohesion"
---

<objective>
Add F2 (vote-based compass toggle) to the compass page and F3 (cohesion columns/toggle) to the rankings page. Both consume the shared `votes-api.ts` client and i18n keys created in 14-05. Neither file touches the locale JSON (keys already exist), so this plan runs in parallel with 14-06.

Purpose: Let users switch the compass between "what they say" (text) and "what they do" (votes), and expose party/deputy cohesion metrics on the rankings page.
Output: modified compass page + rankings page.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md

<interfaces>
From frontend/src/lib/votes-api.ts (created in 14-05):
  getVoteCompass(legislature: number, chamber: string): Promise<VoteCompassData>
    VoteCompassData { available, reason?, parties?: [{party, x, y}], variance_explained?: number[] }
  getVoteCohesion(chamber: string, legislature: number, deputyId?: string): Promise<VoteCohesionData | DeputyVoteStats>
    VoteCohesionData { available, parties?: [{party, rice, votes_sampled}] }

i18n (added in 14-05): useTranslations('VoteCompass') → toggleText, toggleVote, tooltip, variance, unavailable
                       useTranslations('Cohesion')  → columnRice, columnRebellion, columnParticipation, toggle, unavailable

compass/page.tsx: CompassPage() with compassData state (CompassData), fetchCompass(topic), form submit.
  Existing render uses CompassCard / AxisSummary. Add a mode toggle: "text" (default) | "vote".
rankings/page.tsx: RankingPage() with deputies state (RankingDeputy[]), fetchRanking(topic),
  filter bar, RankingRow component, sortBy state. Chamber/legislature: rankings currently posts {topic}
  only — read the file; use default chamber="camera", legislature=19 for the cohesion lookup (or reuse
  any chamber/legislature state if present).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: F2 — vote/text axis toggle on the compass page</name>
  <read_first>
    - frontend/src/app/compass/page.tsx (full component: state, fetchCompass, render tree, CompassCard usage, AxisSummary)
    - frontend/src/lib/votes-api.ts (getVoteCompass signature)
    - frontend/src/types/votes.ts (VoteCompassData)
    - frontend/src/components/chat/CompassCard.tsx (CompassData shape — to understand how coordinates render, so the vote compass can reuse or adapt the scatter rendering)
  </read_first>
  <action>
    In `compass/page.tsx`:
    - Add `const [axisMode, setAxisMode] = useState<"text" | "vote">("text");` and `const [voteCompass, setVoteCompass] = useState<VoteCompassData | null>(null);` and `const tvc = useTranslations('VoteCompass');`.
    - Add a segmented control near the top of the results area: two buttons `tvc('toggleText')` and `tvc('toggleVote')`, styled editorial (segmented, matching app styling — grep existing segmented/toggle usage). Include the thesis tooltip `tvc('tooltip')` on hover/info icon.
    - When the user switches to "vote" mode (and on first switch), call `getVoteCompass(legislature, chamber)` and store in `voteCompass`. Use existing chamber/legislature values from the page if present; otherwise default `legislature=19, chamber="camera"` (vote compass must NOT use "both" — mixing chambers is invalid per Pitfall 1; if the page has a chamber selector set to "both", pass "camera").
    - Render:
      - `axisMode === "text"` → existing text compass render (UNCHANGED).
      - `axisMode === "vote"` → if `voteCompass?.available`, render a scatter of `voteCompass.parties` (x,y) reusing the compass scatter visual (party labels at coordinates) and show `tvc('variance')` with `variance_explained`. If `available === false`, show `tvc('unavailable')`.
    - Text compass remains the default mode and its code path is not modified.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && grep -q "getVoteCompass" src/app/compass/page.tsx && grep -q "axisMode" src/app/compass/page.tsx && npx tsc --noEmit -p tsconfig.json && npx eslint src/app/compass/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `compass/page.tsx` contains `getVoteCompass`, `axisMode`, `useTranslations('VoteCompass')`
    - `compass/page.tsx` contains a branch for `voteCompass` `available === false` rendering `tvc('unavailable')`
    - default state is `"text"` (grep `useState<"text" | "vote">("text")`)
    - `npx tsc --noEmit` exits 0 and eslint on compass/page.tsx reports no errors
  </acceptance_criteria>
  <done>Compass page toggles between text (default) and vote axes; vote mode loads coordinates and degrades gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: F3 — cohesion columns/toggle on the rankings page</name>
  <read_first>
    - frontend/src/app/rankings/page.tsx (full component: deputies state, RankingRow, columns, sortBy, filter bar; whether chamber/legislature state exists)
    - frontend/src/lib/votes-api.ts (getVoteCohesion signature)
    - frontend/src/types/votes.ts (VoteCohesionData, PartyCohesion)
  </read_first>
  <action>
    In `rankings/page.tsx`:
    - Add `const [showVoteMetrics, setShowVoteMetrics] = useState(false);`, `const [cohesion, setCohesion] = useState<VoteCohesionData | null>(null);`, `const tc = useTranslations('Cohesion');`.
    - Add a toggle button `tc('toggle')` ("Metriche di voto") in the filter/controls bar. When enabled (and cohesion not yet loaded), call `getVoteCohesion(chamber, legislature)` — use page chamber/legislature state if present, else default `chamber="camera", legislature=19`. Cast the result to `VoteCohesionData` (party mode).
    - When `showVoteMetrics` is true and `cohesion?.available`: build a `party → rice` lookup from `cohesion.parties` and render a new "Coesione (Rice)" value on each RankingRow (or an added column) by matching the deputy's party to the lookup. Header uses `tc('columnRice')`.
    - When `cohesion?.available === false` (or toggle off): DO NOT render the vote columns at all (hide, not zero — Pitfall 3). Show `tc('unavailable')` as a small note if the user enabled the toggle but data is absent.
    - Keep existing authority columns and sorting untouched; the vote metrics are additive.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && grep -q "getVoteCohesion" src/app/rankings/page.tsx && grep -q "showVoteMetrics" src/app/rankings/page.tsx && npx tsc --noEmit -p tsconfig.json && npx eslint src/app/rankings/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rankings/page.tsx` contains `getVoteCohesion`, `showVoteMetrics`, `useTranslations('Cohesion')`
    - `rankings/page.tsx` gates vote-column rendering on `cohesion?.available` (hidden when false)
    - `rankings/page.tsx` references `tc('columnRice')`
    - `npx tsc --noEmit` exits 0 and eslint on rankings/page.tsx reports no errors
  </acceptance_criteria>
  <done>Rankings page shows Rice cohesion (and rebellion/participation-ready) columns behind a toggle, hidden entirely when individual vote data is absent.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` green; `npx eslint src/app/compass/page.tsx src/app/rankings/page.tsx` clean
- Compass toggle defaults to text; vote mode calls /api/compass/votes
- Rankings vote columns hidden when available:false
</verification>

<success_criteria>
F2 compass toggle and F3 cohesion columns are live, consume the shared client + i18n, and degrade by hiding when data is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-07-SUMMARY.md`
</output>
