---
phase: 14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page
plan: 08
type: execute
wave: 5
depends_on: ["14-05", "14-06", "14-07"]
files_modified:
  - backend/tests/unit/test_vote_services.py
autonomous: false
requirements: [VI-01, VI-02, VI-03, VI-04, VI-05]
must_haves:
  truths:
    - "The full backend unit suite passes"
    - "The frontend type-checks and lints clean"
    - "All five features are verified against the live DB by a human (data-dependent behaviors)"
  artifacts:
    - path: "backend/tests/unit/test_vote_services.py"
      provides: "Any final regression assertions added during verification"
      contains: "def test_"
  key_links: []
---

<objective>
End-to-end verification of Phase 14. Run the automated gates (backend pytest + frontend tsc/eslint), then a human checkpoint to confirm the five features behave correctly against the live database — because F1/F2/F3 are data-dependent (IndividualVote coverage is partial as of 2026-07-05: Camera XIX 69/391 deputies, Senato 0) and must degrade gracefully.

Purpose: Prove the phase goal is achieved and that degradation paths render correctly with real, partial data.
Output: green automated gates + human sign-off.
</objective>

<execution_context>
@/Users/mirkotritella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mirkotritella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-CONTEXT.md
@.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gates — full backend suite + frontend tsc/eslint + DISCUSSES-linkage probe</name>
  <files>backend/tests/unit/test_vote_services.py</files>
  <read_first>
    - backend/tests/unit/test_vote_services.py
    - backend/tests/unit/test_vote_compass.py
    - .planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-RESEARCH.md (Open Question 1 — DISCUSSES count probe)
  </read_first>
  <action>
    1. Run the full backend unit suite and the frontend gates. All must be green.
    2. Probe the DISCUSSES linkage count (Open Question 1 in research) so the human check knows whether F1/F4 act-linked paths will trigger. If a live Neo4j is reachable, run (via an existing script or a one-off using the project's Neo4jClient):
       `MATCH (d:Debate)-[:DISCUSSES]->(a:ParliamentaryAct) RETURN count(*) AS n`
       and record the number in the SUMMARY. If Neo4j is not reachable in this environment, note that and defer the probe to the human checkpoint (Task 2 step).
    3. If any automated gate fails, fix forward within this plan (small regressions only) or flag for a gap-closure plan.
  </action>
  <verify>
    <automated>cd /Users/mirkotritella/Desktop/ParliamentRAG/backend && python -m pytest tests/unit/ -x -q && cd /Users/mirkotritella/Desktop/ParliamentRAG/frontend && npx tsc --noEmit -p tsconfig.json && npx eslint src/app/votes/page.tsx src/app/compass/page.tsx src/app/rankings/page.tsx src/components/chat/MessageBubble.tsx src/components/layout/Sidebar.tsx src/hooks/use-chat.ts src/lib/votes-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/ -x -q` exits 0
    - `npx tsc --noEmit` exits 0
    - eslint on all listed Phase-14 frontend files reports no errors
    - DISCUSSES count recorded in the SUMMARY (or explicitly noted as deferred to human checkpoint)
  </acceptance_criteria>
  <done>All automated gates green; DISCUSSES linkage count recorded or deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human E2E verification of all five features against live DB</name>
  <files>none</files>
  <action>Pause for the user to manually verify F1–F5 against the live database following the how-to-verify steps. This is a blocking checkpoint: do not proceed or mark the phase complete until the user responds with the resume-signal.</action>
  <what-built>
    F1 vote-coherence chat block, F2 vote/text compass toggle, F3 cohesion columns, F4 vote facts in RAG answers, F5 votes explorer page — all wired to the live backend.
  </what-built>
  <how-to-verify>
    Start backend + frontend against the live Neo4j, then:

    1. F5 — Visit http://localhost:3000/votes. Confirm: the table loads, filters (chamber, legislature XIX/XVIII, date range, outcome, margin) change results, labels show act/debate titles (NOT the literal word "Votazione"), and clicking a row navigates to the debate transcript/timeline. Sidebar "Votazioni" entry under Strumenti is present and active on /votes.

    2. F2 — Visit http://localhost:3000/compass. Confirm the "Assi testuali | Assi di voto" toggle exists, text is the default, and switching to "Assi di voto" loads party coordinates (Camera XIX). If individual-vote data is insufficient, confirm it shows the unavailable message rather than all parties at the origin.

    3. F3 — Visit http://localhost:3000/rankings. Enable the "Metriche di voto" toggle. Confirm Rice cohesion appears for parties with individual-vote data, and that columns are HIDDEN (not shown as 0%) for chambers/legislatures without individual votes (e.g. Senato).

    4. F1 — Ask a chat question about a topic that was voted on (e.g. a recent DDL / bilancio). Confirm a "Come hanno votato" block renders under the relevant citation group: overall Esito + per-party chips when data exists, or aggregate-only outcome when individual data is missing.

    5. F4 — In the same answer, confirm the model references the vote outcome verbatim (e.g. "approvato il {date} con {n} sì, {n} no") and that a vote chip linking to the debate is present (not treated as a hallucinated citation).

    Note the DISCUSSES count from Task 1: if it is low, F1/F4 act-linked blocks will trigger rarely — that is expected, not a bug.
  </how-to-verify>
  <resume-signal>Type "approved" if all five features behave correctly (including graceful degradation), or describe any issues to route into a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- Automated: backend unit suite green, frontend tsc + eslint green
- Manual: all five features verified against live DB, degradation paths confirmed
</verification>

<success_criteria>
Phase 14 goal achieved: vote graph is surfaced as F1–F5 user-facing intelligence, all filtering by chamber+legislature, degrading gracefully where IndividualVote data is partial.
</success_criteria>

<output>
After completion, create `.planning/phases/14-vote-intelligence-speech-vote-coherence-vote-based-compass-axis-cohesion-indices-vote-facts-in-rag-votes-explorer-page/14-08-SUMMARY.md`
</output>
